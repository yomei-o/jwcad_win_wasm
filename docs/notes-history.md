# これまでの並び —— 古い「次にやること」

RESUME.md の頭にあった作業の並びです。**取り消し線のものは入ったもの**で、
どういう順に何が解けていったかの記録として、そのまま残してあります。
いまの残項目は RESUME.md の「残っているもの」を見てください。

### 次にやること（この順で）

1. ~~**ネイティブ版に本物のメニューを付ける。**~~ **入りました。**
   `src/main_win32.c` の `build_menu()` が `src/gen/menu.h` の木 176 項目を
   そのまま `CreateMenu` / `CreatePopupMenu` / `AppendMenuW` に流して
   `SetMenu` します（名前は CP932 なので `jw_to_utf16` で UTF-16 に）。
   `WM_COMMAND` は `do_command()` から `app_command(id)` へ ——
   ツールバーのボタンと同じ入口で、ファイルが要るものは同じ
   `app_take_action()` を通ります。`WM_INITMENUPOPUP` でいまのコマンドに
   チェックを付けるのは原典の `ON_UPDATE_COMMAND_UI` と同じ振る舞いです。
   **クライアント領域は 1264×741 のまま**で（`AdjustWindowRectEx` の
   `bMenu` を `TRUE` に）、窓ごと撮って原典と突き合わせても
   **文字の外は 0 画素差**のままでした。
   ショートカットも原典の ACCELERATOR 資源 320 から
   `tools/mkaccel.py` → `src/gen/accel.h` → `CreateAcceleratorTableW` で
   18 個入れてあります（メニューが Ctrl+N と書いているのは資源にそう
   書いてあるからで、効かせるにはこの表が要ります）。

   **ブラウザ版も入りました。**同じ木で、ポップアップを自分で描いて
   押せます（`src/ui.c` の `ui_popup_*`、`app_chrome_press` /
   `app_chrome_move` が入口、`tests/menu_test.c` が検査）。名前を押すと
   開き、もう一度押すと閉じ、外を押すと**閉じるだけで何もしない**、
   サブメニューは親の右に開く、いまのコマンドには印が付く。
   ページ側は何も直していません —— `index.html` はもともと canvas の
   生の座標を `jw_press` / `jw_move` に渡していて、`app_chrome_h()` より
   上なら chrome だと分かるからです。

   **寸法は原典から取りました。**`tools/jwdraw.ps1` の `menu:<文字>,<png>`
   段が `WM_SYSCOMMAND SC_KEYMENU` を投げて、開いた `#32768` を
   `PrintWindow` で撮ります。7 枚の窓の高さを項目数・区切り数と連立させて
   解くと、**項目 22 行・区切り 9 行・枠 3・余白 0** できれいに合いました
   （`ファイル` だけ 2 項目多く出ますが、これは MFC が差し込む
   「最近使ったファイル」です）。位置はフレームのクライアント座標で
   名前の x−8、y=−1。色は本体 `#f9f9f9`・枠 `#e5e5e5`。

   **ポップアップは画素比較しません。**キャプションや窓枠と同じで、原典の
   ものはテーマ付きの窓（角丸・影・別のフォント）なので合わせようがない、
   という既定の扱いです。写したのは寸法だけです。
2. ~~**円弧の 1 画素ずれ。**~~ **入りました（2026-09-25）。**原典は
   **実線の円弧だけ**を GDI の `Arc` に渡し、線種が付いていれば
   **弦の折れ線**にして、直線と同じ線種の描き手に渡していました。
   詳しくは下の「答えが出ました」。`天空率表.jww` は 1,487 → **132**、
   15 枚の合計は 4,445 → **1,933** 画素。以下はそこに至るまでの記録です。

   **外接矩形の作り方は分かりました。**`Arc` を呼ぶのは `FUN_004b5090`
   （`CDC::Arc` の下、`FUN_004b50e0` から）で、箱を作っているのは
   `FUN_00421490` です。5 か所すべてが

       FUN_00416040(cx - r, cy - r, cx + 1 + r, cy + 1 + r)

   の形で、**中心 1 点と半径 1 つ**からできています。四隅を別々に丸めて
   いるのではありませんでした（まるごとの円だけは `+1` が付かず、
   しかも `Ellipse` ではなく `Arc` を 2 回です —— 下の「真円は
   `Ellipse` ではなく `Arc` 2 本」）。中心は `FUN_004b6d60`、半径は
   `FUN_004b8250` で、

       中心 x = (int)((mx - ox) / mmpp) + bx      ← 切り捨て
       中心 y = by - (int)((my - oy) / mmpp)      ← 切り捨て
       半径   = (int)(r / mmpp + 0.5)             ← 四捨五入

   です。移植の `jw_sx` / `jw_sy` / 円弧の `rp` はもともとこの形でした。

   **違ったのは割り算か掛け算かだけでした。**原典は**画素あたりのミリ**で
   持っていて割ります（`FUN_004b6d60` も `FUN_004b8250` も除算）。移植は
   逆数を持って掛けていました。同じ値ではないので、境界近くの座標では
   1 画素ずれ得ます。**原典と同じ割り算に直しました**（`jw_view.mmpp`）。
   ただし **Test1 の 193・Test7 の 39 は 1 画素も動きませんでした** ——
   この 2 枚では効かない、というところまでです。

   **箱の作り分けも合っていました。**`FUN_00421490` には
   `FUN_00416040` が 2 か所あって、`|振り角| > 6.283185207179586`
   （2π より 1e-7 だけ小さい値）なら `(cx-r, cy-r, cx+r, cy+r)`、
   そうでなければ `+1` の箱です。移植の `odd` の分け方と同じでした。
   ついでに、**半径が 2 未満なら円を描かず点を打ちます**
   （`if (local_e8 < 2)` の枝、`FUN_004b8940` → `FUN_004b7a70`）。
   移植はこれをしていません。**（あとで入れました —— 下の
   「2 画素に満たない円は点です」。）**

   **正体は位相ではなく「箱」でした（直しました）。**破線の円だけを 6 つ
   置いた図面を作って原典に描かせ、実線版と並べて測ったら、はっきり出ました:

   | | 中心 | 平均半径 |
   |---|---|---|
   | 原典・実線 | 435.50, 278.49 | 64.32 |
   | 移植・実線 | 435.50, 278.50 | 64.31 |
   | 原典・**破線** | 436.54, 279.30 | **65.11** |
   | 移植・破線（当時） | 435.50, 278.52 | 64.32 |

   **原典は、同じ円を実線で描くときと破線で描くときで別の環に置きます。**
   実線は `2r` の箱、破線は**弧の `2r+1` の箱**です（破線の円は弧として
   描かれている、と読むのが自然です）。移植は両方とも `2r` にしていたので、
   環そのものが 1 画素ずれていました —— 刻みの位相をどういじっても直らない
   はずです。`lt != 1` なら `odd = 1` にして、**`天空率表.jww` が
   2,451 → 2,016 画素**、15 枚の合計が 9,887 → **9,452** になりました。
   （**この `lt != 1` は後で外しました。**下の「答えが出ました」の通り、
   破線の円が別の環に乗るのは箱の違いではなく、**そもそも GDI の `Arc` を
   通っていないから**でした。）

   **潰した仮説**（どれも効きませんでした。同じ道を通らないように）:
   位相を ±3 ずらす（0 が最良）、位相を歩数でなく弧長で進める（悪化）、
   円周に整数個のパターンを収める（ほぼ不変）。

   **残りはまだ環がぴったりではありません。**原典の破線の環は**半径が
   63.9〜66.5 に広がっていて**、単一画素の環ではありません（実線は
   63.7〜64.9）。線種違いの 3 円を重ねても同じでした。刻みごとに別々の
   `Arc` を呼んでいて**端が個別に丸められている**、と読むのがいちばん
   自然です。そこまで合わせるには GDI の `Arc` を短い弧について再現する
   必要があり、大仕事です。

   **ここでも潰した仮説**: 幾何ペン（`ExtCreatePen` の
   `PS_GEOMETRIC|PS_JOIN_BEVEL`）——幅 1 では環は素のペンと同じ画素でした
   （`tools/gdiarc.c` に `style+100` で試せる口を付けてあります）。
   整数画素のずらし（±1 の 4 方向すべて悪化、0 が最良）。

   `JW_ARC_WALK=1` で境界の歩きを書き出せるので、続きはそこから。

   （以前の記録）**残差は線種の位相**`天空率表.jww` の残り 2,451 を
   拡大して原典と並べると、円弧そのものは合っていて、**破線の刻みが
   弧に沿って少しずれています**（`tmp/refs/d09.png` と
   `tmp/d09.out.png` の 320,160 あたり）。1 画素ずれが 1,874／2,451 と
   多いのもこれで説明が付きます。**外接矩形ではありません。**

   **答えが出ました（2026-09-25）。原典は破線の円弧を折れ線で描いています。**
   `FUN_00421490` は線種の表 `doc+0x2fc4` を引いて、そこが **-1（実線）の
   ときだけ** GDI の `Arc` を呼びます。それ以外は `FUN_0042e140` に飛び、

       p = 楕円上の点(始角);
       for (t = 刻み; |振り角| > |t|; t += 刻み) { q = 点(始角+t); 線(p,q); p = q; }
       線(p, 点(始角+振り角));          ← 最後の 1 本

   と**弦を並べます**。弦は `FUN_00436a20` →
   `FUN_004bbef0`（直線と同じ線種の描き手）に渡され、そのとき

   * **第 5 引数に 1 を渡します**（直線は 0）。これが**「線に合わせて
     伸縮する」のを止める**枝で、線種は素の大きさのまま走ります。
   * **第 4 引数がパターンの 32 ビットのレジスタで、弦をまたいで
     持ち回されます**（最上位ビットを見て、1 ビットずつ左回転）。

   つまり**刻みは弦の上で進み、角では止まりも戻りもしません**。だから
   点数でも弧長でも角度でも合わなかったのです。

   **刻み幅は「画面での大きさ」ではなく「図面のミリ」で決まります。**
   長いほうの半径 × `doc+0x1728` を階段で引きます ——
   50 未満なら 0.2、125 未満なら 0.1、312 未満なら 0.07、以下 781 で 0.04、
   1953 で 0.02、4882 で 0.01、12200 で 0.005、24400 で 0.002、
   それ以上は 0.001 ラジアン。`doc+0x1728` は画素あたりのミリの
   **分子**（`FUN_004b6d60` は `doc[6000]/doc[0x1728]` で割ります）なので
   画面では 1 です。**半径 100 mm の円弧は、どれだけ拡大しても 0.1 刻み
   （一周 63 本）**。印刷のときだけ原典が刻みを細くする
   （`4/パターン長`）のも、ミリの階段が紙の細かさを知らないからです。

   移植もそうしました（`src/draw.c` の `arc` と `line`）。**15 枚の合計が
   4,445 → 1,933 画素**、`天空率表` は 1,487 → 132、`Test1` は 193 → 28。

   ついでに分かったこと:

   * **実線の円と破線の円で環が違う**のは、片方が GDI の `Arc`、
     もう片方が折れ線だからでした。`odd`（`2r+1` の箱）の分け方から
     線種の条件を外して、**実線かどうか**だけで分けています。
   * 弦の遠い端は**次の弦のもの**です（`open` のまま）。円弧全体の
     最後の 1 点だけは、線種が何であろうと必ず打たれます
     （`FUN_00436a20` の `param_1 != 0` の枝。太いペンでは打ちません）。
   * 走りは**弦の端で止めます**。移植は `nbits` をパターン 2 つぶん
     多く回していて、弦ごとに数画素はみ出していました（2,385 → 2,278）。

   **潰した仮説**（同じ道を通らないように）: 位相を ±3 ずらす（0 が最良）、
   位相を弧長で進める、円周に整数個のパターンを収める、幾何ペン
   （`ExtCreatePen` の `PS_GEOMETRIC|PS_JOIN_BEVEL`）、整数画素のずらし。
   **階段の入力を画素にする**のも違いました —— 半径 163 画素
   （= 100 mm）の円弧は 0.07 ではなく 0.1 で原典と揃います。

   GDI 側の道具（`tools/gdiarc.c`、`decomp/res/arcdash.txt`、
   `tools/walk.exe`）はそのまま置いてあります。GDI が破線の `Arc` を
   どう刻むかは**原典と関係がありませんでした** —— 原典は破線の弧を
   GDI に渡していないからです。

2. ~~**包絡処理。**~~ **入りました**（`src/houraku.c`、範囲内消去も
   含めて 13 通りとも原典と一致）。円弧は原典も包絡しません。
   残るのは傾いた枠・中間消去（どちらも駆動できていません）。

3. ~~**SFC の書き出し。**~~ **入りました**（`src/sfcwrite.c`）。
   原典の書いた 4 つと 1 バイト違いません（楕円弧も）。

4. ~~**JWC の書き出し。**~~ **入りました**（「JWC」の節）。頭の CSV の
   5 つ（5・6・11・12・19 番目）は読めていないので原典のものを焼いて
   います。

5. ~~**DXF の HATCH。**~~ **入りました**（塗りつぶしのぶん。境界が
   円弧のものは円ソリッドになるので、そこだけまだです）。

### ここから先（2026-09-25 の時点）

**ファイルの出し入れは 6 形式**（.jww・DXF・SFC・JWC・.jws・座標ファイル）
**とも読み書き揃い、コマンドも主なものは入りました。**
`sh tools/check.sh` は 1,267 項目（同梱の図面 16 枚を**ネイティブと
WASM の両方で描いて 1 画素ずつ**突き合わせる段も入りました —— 16 枚とも
同じです。前は Test1 と Test7 だけでした）。

**画素の残りは 814。**そのほとんどが**部分円弧の環**で、原典はそれを
GDI の `CDC::Arc` に渡しています。環の表は真円も部分円弧も `Arc` 2 本から
取り直しました（下の「部分円弧の環も `Ellipse` ではありませんでした」）。
残るぶんは GDI の弧が**両端の位置で環まるごとが変わる**からで、表にも
漸化式にもできません（下の「GDI の `Arc` は GDI の `Ellipse` と別の環を
描きます」）。

**残った画素を調べる道具**が `tools/why.py` と `tools/arcrp.py` です
（`JW_SHOT_ELEMS=1 ./tests/shot.exe …` が要素の落ちた場所を書き出します）。
**壊し方の試験**は `tests/fuzz_test.c`（壊れたファイル 95 万通り）と
`tests/cmdfuzz_test.c`（コマンドを手当たり次第に 204 万手）。

**サニタイザに通す道具**が `tools/asan.sh` です（2026-09-25 に入りました）。
w64devkit には `libasan` がありませんが emscripten の clang にはあるので、
試験をまるごと node 向けに組んで **AddressSanitizer と未定義動作の
サニタイザ**に通します。**普通の Windows 版では 787 万通り流しても
出ない**ものが**8 つ**出ました —— 読み手の穴 2 つ、`.jws` の頭の
写しすぎ、境目の合わない短整数、**狭い窓で画布の外に書く**（4 か所）、
筆 10 の表外れ 2 つ、`int` に入らない `double`、そして
**ブラウザが渡すファイル名で見出しの枠の外に書く**。
詳しくは下の「AddressSanitizer に読ませたら」からの 4 節。
**読み手・作図・入口をいじったら回してください。**

**サニタイザが見つけていないものも 4 種類あります**（2026-09-26、
どれも原を読んで見つけました。下の「紙のミリを画素に直すところ」
からの 4 節）:

* **円の四分円の置き場**が半径 5,792 から溢れます（拡大すれば届く）
* **`(int)` そのものが未定義**になる箇所（座標は ±1e12 まで通るので、
  壊れていない読み方でも 2e12 が来ます）
* **返ってこない輪** —— 窓から遠い線が 2 億歩、目盛が 1e-300 刻み、
  1 文字が 1 億×1 億、角度を 1 周ずつ畳むのが 1,600 億周
* **`app_zoom` が `mmpp` を double の外に出す**と、そのあとの座標が
  全部 NaN になります

**乱打の網は、帯の狭いものには届きません。**円の四分円の溢れは
半径 5,792〜8,189 という 1.4 倍の帯でだけ起きるので、39 万通り×
198 ファイルを ASan で通しても**報告ゼロ**でした（実測）。
こういうものは**定義域を端から端まで歩く試験**を書くほうが早く、
`tests/bigcirc_test.c` が半径 1〜8,200 を全部通します。
`tests/fuzz_test.c` の `monsters()` にも、**角度が 9e11 ラジアンの
円弧**を 1 つ置いてあります —— 197 個のファイルのどれも、そこまでは
届きません。

残っているものを、やりやすい順に:

**駆動できるもの（続きはここから）**

* **接線点 (32912)** —— 送ると「基準点を指示して下さい」になり、基準点を
  1 つ押すとコマンドが始点から仕切り直しになります。そのあと円を読ませても
  終点にならず、使い方が掴めていません（下記）。
* **座標ファイルの残り** —— 読み書きとも入りましたが、ブロックの参照は
  書いていません。
* **AUTOモード (32915)・外部変形 (32913)・2.5D (32937)・日影図 (32943)・
  天空図 (32971)** —— どれも手つかず。AUTO モードの割り当ては
  `HKCU\Software\Jw_cad\jw_win\AutoMode` の `LD_AM*`・`RD_AM*` に
  入っています。

**駆動できないと分かったもの**

* **建具平面/断面/立面 (32848/32866/32865)・線記号変形 (32869)・
  ハッチの 図形 (1693)** —— 「ファイル選択」窓の .DAT 側は
  「リスト表示」が無効で、投げ込んだクリックもダブルクリックも Enter も
  効きません（下記）。フォルダ鍵は見つけてあります。
* **印刷 (57607)** —— Windows の共通ダイアログ（プリンター名が機械ごとに
  違う）。**レイヤ一覧 (32917)** —— コモンコントロールずくめ。
  どちらも画素で合わせる相手になりません。
* **包絡の中間消去** —— 原典が `GetKeyState` を読むので駆動できません。

**残っている食い違い**

* ~~**破線の円弧の画素**~~ **入りました**（下記「答えが出ました」）。
  原典は破線の円弧を GDI に渡さず、**弦の折れ線**にしてから線種を
  かけていました。15 枚の合計が 4,445 → **1,933** 画素。
* **レイヤの格子の印を直しました**（下記）。**15 枚の合計が 1,933 →
  1,416 画素。**「2652 の枠は左上の升」ではなく「**書き込んでいる升**」
  でした（Test6 は グループ 4 に書いています）。ついでに、グループの升の
  黒い枠は**書ける（状態 2 以上の）グループだけ**です。
* **仮点の形**（十字・3×3・1 画素）、**レイヤの印の左右**、
  **表示のみレイヤを先に描く**の 3 つも入りました（下記）。
  15 枚の合計は **814 画素**、**5 枚は 1 画素も違いません**。
* 残っているのは **ほとんどが円弧**です ——
  `Ａマンション平面例` の 392、`日影図` の 275、`サンプル` の 70。
  `tools/why.py` で分けると「移植が打った」と「原典が打った」が
  ほぼ半々、つまり同じところを 1 画素ずらして打っています。
  **環の表は真円も部分円弧も GDI の `Arc` 2 本から取り直しました**
  （下記「真円は `Ellipse` ではなく `Arc` 2 本」「部分円弧の環も
  `Ellipse` ではありませんでした」）。**終わりの画素を塗らない**のも
  入りました（下記）。残っているのは大きく 2 つで、

  - **`日影図` の円 1 つ（223 画素）は「円弧の 2r+1 の枠」**で
    描かれています。そこに入れると 275 → 60 ですが、同じことを
    `天空率表` にすると 14 → 2,751 で壊れます。**円ごとに枠が違い、
    その決め手がまだ分かりません**（下記）。
  - **`Ａマンション平面例` は端の切り方**が 6 割
    （掃引の外 152・弧の端 100）。GDI の弧は両端の位置で環まるごとが
    変わるので、ここから先は GDI の中身の話です（下記）。
* **まだ説明がつかないもの:** `Ａマンション平面例` のいくつかの円弧で
  **原典の環の半径がちょうど 1 画素大きい**（要素 1656 は
  `r/mmpp + 0.5 = 24.06` で移植は 24、原典は**半径ちょうど 25 の
  中点円**）。GDI の弧の刻み方の違いでは説明できません —— 25 の環は
  原典が渡したはずの枠（±24）の**外**に出ます。部分円弧を一律に +1 に
  すると 973 → 2,119 で悪くなるので、一部だけです。
  **筋の候補**: その要素を原典は描いておらず、25 の環は移植が
  描いていない別の要素（建具など）のもの、という見方もできます。

以下は古い並びのまま残してあります:

1. **ハッチの 図形 (1693)**（「ハッチ」の節）。図形ファイルの
   ダイアログを駆動する必要があります。**図形を読み込んでおいても、
   ピッチを入れても、何も描かれませんでした**（下記）。
2. **文字種ダイアログのコンボ** —— **色 No. (2358) は入りました**（下記）。
   フォント名 (1423) はまだです（機械ごとに中身が変わるので、
   突き合わせる相手がありません）。
3. ~~**破線の円弧の画素**~~ **入りました**（上の通り）。
4. ~~**属性選択・属性変更の「指定」もの**~~ **入りました**（下記）。
   「指定」は **OK を押すと出てくる 線属性 ダイアログで選んだ色・線種**
   でした。ハッチ／図形／寸法／建具／曲線の属性指定は
   +0x44 のビットが未解明のままです。
5. **包絡の中間消去** —— 駆動できないので前提から。
   （**包絡の枠は軸角に従いませんでした**。下記。）
6. **ブロックはひと通り入りました**（化・解除・属性・編集・名前変更・
   反映先の選択）。
7. **ファイルを開くものたち** —— **図形読込 (32862) と 図形登録 (32946)
   は入りました**（下記。窓の駆動の仕掛けも `figin:`・`figout:` として
   `tools/jwdraw.ps1` に入っています）。残りは 線記号変形 (32869)・
   建具平面/断面/立面 (32848/32866/32865)・ハッチの 図形 (1693)。
   **同じ仕掛けで駆動できるはずです。**
8. **レイヤ一覧 (32917)** —— 1110x640 の窓で、中に `SysTreeView32` と
   `SysListView32`、それに「ファイル選択」という子窓がいくつも入って
   います（16 レイヤの中身を縮小して並べるもの）。コモンコントロール
   ずくめなので、画素で合わせるのは無理筋です。
   （基本設定は入りました。下記。）
