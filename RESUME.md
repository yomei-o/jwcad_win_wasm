# 引き継ぎ

Jw_cad 10.03.6 を逆コンパイルから C に書き直したものです。**何が動いて
いて、何が残っているか**がこの頁で、**どう分かったか**は
[`docs/`](docs/) の 6 冊のノートにあります（下の「ノート」）。

## いまどこまで

**CAD として一通り使えます。**線を引き、消し、角を作り、複線を出し、
文字を置いて、6 形式で保存できます。ネイティブ（`jw_port.exe`）と
ブラウザ（<https://yomei-o.github.io/jwcad_win_wasm/>）の両方が動きます。

| | |
|---|---|
| 絵 | 同梱の図面 15 枚で**残り 524 画素**（文字の枠は除く）。5 枚は 0 画素 |
| 枠 | メニュー・ツールバー・コマンドバーとも**文字を除いて 0 画素差**。窓の大きさを変えても追従します |
| native と WASM | 16 枚とも**互いに 0 画素差** |
| 試験 | `sh tools/check.sh` が **1,267 項目 ok** |
| 形式 | `.jww`・DXF・SFC・JWC・`.jws`・座標ファイル —— **6 つとも読めて書けます**（原典の出すものと 1 バイト違わず） |
| コマンド | 線・円・弧・点・文字・消去・コーナー・線伸縮・複線・範囲選択/複写/移動・寸法・多角形・面取・分割・２線・中心線・ハッチ・曲線・包絡・属性変更・データ整理・ブロック一式・読取・元に戻る／進む |

**答えはどれも原典に作らせています。**同じ操作を原典にさせて保存させた
`.jww` を 1e-9 まで突き合わせる（`tests/*_test.c`）か、原典の描いた画面を
1 画素ずつ突き合わせる（`tools/refshots.sh` → `tools/scoreall.sh`）か、
どちらかです。**自分の出力を答えにしたものは 1 つもありません。**

## 残っているもの

### 1. 絵の 524 画素 —— ほぼ全部が部分円弧

| 図面 | 画素 | 中身 |
|---|---:|---|
| `Ａマンション平面例` | 392 | 部分円弧の環。GDI が **2r+2 の枠**で描いているように見えるものが 13 本中 9 本 |
| `サンプル` | 70 | 同じく部分円弧。ただしこちらは**測れた 8 本とも 2r+1**（移植と同じ） |
| `Test6` | 23 | |
| `木造平面例` | 14 | |
| `日影図` | 8 | |
| そのほか 10 枚 | 17 | |

**原因が分かりました（2026-09-26）。移植の作りが原理的に合いません。**
移植は部分円弧を、`src/gen/circle.h` に焼いた**真円の環**から両端のあいだを
切り出して描いています。しかし **GDI の部分弧の環は、同じ枠の真円の環の
部分集合ではありません** —— 224 本のうち 213 本が、真円の環に無い画素を
塗ります（`tools/arcsub.py` が GDI 自身に両方を聞いたもの。原典も基準画像も
要りません）。図面が使う半径でも起きます —— 半径 24 で 64 本中 51 本、
1 本あたり平均 5 画素。

これで、説明できなかったことが**ひとつ残らず**片付きます:

* 半径も掃引も傾きも同じ 2 本（弧 53 と 846）が違う環になる ——
  **始点が違えば環が違う**から
* `サンプル` で移植が取りこぼす画素が、端ではなく**弧の途中**にある
* 弧 1656 の環が 1 画素ぶん外にある

**直すには GDI の歩きそのものを書き直すしかありません**（環が両端に依る
ので表には持てない）。答え合わせの相手は `tools/gdiarc.c` が用意できます
—— メモリ DC に描かせるだけなので、**原典も対話デスクトップも要りません**。
詳しくは [`docs/notes-pixels.md`](docs/notes-pixels.md) の
「部分円弧の環は、真円の環の部分集合ではありません」。

### 2. まだ書いていないコマンド（どれも駆動できます）

* **接線点 (32912)** —— 基準点を押すとコマンドが仕切り直しになり、使い方が掴めていません
* **座標ファイルの残り** —— 文字の 10 語は入りました（2026-09-26）。
  残るのは**円ソリッドの 4 語**（`sc`・`se`・`so`・`sg`。要素の線色で
  選ばれます）と **`hn`**、それに**ブロック参照 `BL`**です。
  書式は原典から起こしてあります ——
  [`docs/notes-formats.md`](docs/notes-formats.md)の「座標ファイルの語彙」
* **AUTOモード (32915)** —— 割り当ては `HKCU\Software\Jw_cad\jw_win\AutoMode` の `LD_AM*`・`RD_AM*`
* **外部変形 (32913)・2.5D (32937)・日影図 (32943)・天空図 (32971)** —— 手つかず

### 3. 開けない図面がひとつあります

**寸法図形（`CDataSunpou`）の入った .jww を開けません。**基本設定で
「寸法線と寸法値を寸法図形にする」を入れて描いた寸法がそれで、
`src/jww.c` は知らないクラス名に当たると**ファイルごと読むのをやめます**
（入れ子オブジェクトは長さを持たないので飛ばすこともできません）。
`CData3DSen` などの 2.5D の要素も同じです。同梱の図面にも答えの
ファイルにも 1 つも無いので、今日まで気づいていませんでした。
構造は原典から起こしてあります ——
[`docs/notes-formats.md`](docs/notes-formats.md)の「寸法図形の入った図面は
開けません」。**原典に 1 つ描かせるのが先です。**

### 4. 駆動できないと分かったもの（前提から要ります）

* **建具平面/断面/立面・線記号変形・ハッチの 図形** —— 「ファイル選択」窓の
  .DAT 側は「リスト表示」が無効で、クリックもダブルクリックも Enter も効きません
* **包絡の中間消去** —— 原典が `GetKeyState` を読むので、投げ込んだ操作では動きません
* **印刷 (57607)・レイヤ一覧 (32917)** —— Windows の共通ダイアログとコモン
  コントロールずくめで、画素で合わせる相手になりません
* **文字種ダイアログのフォント名 (1423)** —— 機械ごとに中身が変わるので答えがありません
* **属性指定の +0x44 のビット** —— ハッチ／図形／寸法／建具／曲線のぶんが未解明。
  ただし**文字のぶんは分かりました**（0x10 が族、0x20/0x40/0x80/0x8000 と
  0x100/0x200/0x400/0x1000 が語を選ぶ）。Ghidra は `+0x44` を `int*` の
  `+ 0x11` と書くので、`grep '+ 0x44'` では出てきません

### 5. 説明がついていないもの

`Ａマンション平面例` のいくつかの円弧で**原典の環の半径がちょうど 1 画素
大きい**（要素 1656 は `r/mmpp + 0.5 = 24.06`、移植は 24、原典は半径 25 の
中点円）。25 の環は原典が渡したはずの枠（±24）の**外**に出ます。
一律に +1 にすると悪くなる（973 → 2,119）ので、一部だけです。
**その要素を原典は描いておらず、25 の環は移植が描いていない別の要素
（建具など）のもの**、という見方も残っています。

## 進め方

**編集と commit は手元、ビルドと試験は VM（192.168.11.24）で。**

```sh
sh sync.sh 'sh tools/score.sh'    # 15 枚の採点。1 分弱。探索はこれで回す
sh sync.sh 'sh tools/check.sh'    # 全項目。3 分。commit の前だけ
sh sync.sh 'sh tools/probe.sh'    # 一回きりの問い。引用符が壊れないよう**ファイルに書いて**渡す
```

| 道具 | 何をするもの |
|---|---|
| `tools/refshots.sh` → `tools/scoreall.sh` | 原典の画面と 1 画素ずつ比べて点を出す（文字の枠は伏せる） |
| `tools/why.py`・`tools/whyseg.py` | 残った画素が**どの要素のどこ**かを分ける |
| `tools/gdiarc.c`・`tools/boxask.py`・`tools/circask.py` | GDI 本人に環の画素を聞く（メモリ DC。デスクトップ不要） |
| `tools/asan.sh`・`tools/asanseeds.sh` | emscripten の clang で ASan/UBSan に通す（w64devkit には `libasan` がありません） |
| `tools/sweepenv.sh`・`tools/mutate.sh` | 環境変数の旋回と、思いつきの当てずっぽうを機械に回させる |
| `tools/refanswers.sh`・`tools/jwdraw.ps1`・`tools/shot.ps1` | **原典を動かす**もの。対話デスクトップが要ります |

**試験の網。**`tests/fuzz_test.c`（壊れたファイル 95 万通り）、
`tests/cmdfuzz_test.c`（コマンド乱打 204 万手）、
`tests/bigcirc_test.c`（半径 1〜8,200 を全部）、
`tests/linewalk_test.c`（12,636 本）、`tests/clipwalk_test.c`、
`tests/pickwalk_test.c`（全画素×4 モード×2 流儀）。
**乱打の網は帯の狭いものに届きません** —— 円の四分円の溢れは半径
5,792〜8,189 でだけ起きるので、39 万通り×198 ファイルの ASan で報告ゼロ
でした。そういうものは**定義域を端から端まで歩く試験**のほうが早いです。

## ノート

節は書き換えずそのまま移してあります。各ファイルの頭に目次があります。
**ソースの注に出てくる「RESUME.md の…」という参照は、この 6 冊の
どれかを指しています** —— 引かれている見出しで `grep` してください。

| ノート | 中身 |
|---|---|
| [画素を合わせる](docs/notes-pixels.md) | 円弧・点線・仮点・レイヤの格子・ペン・枠の実測・採点の仕組み。**残りの 524 画素の話は全部ここ** |
| [ファイル形式](docs/notes-formats.md) | `.jww`/`.jws`/DXF/SFC/JWC/座標ファイルの読み書き、ブロック、原典の保存の癖 |
| [`.jww` の形](docs/format-jww.md) | 図面ファイルの構造（`CJw_winDoc::Serialize` の読み写し） |
| [コマンドとダイアログ](docs/notes-commands.md) | 45 節。各コマンドの実装と、原典から読み出した数値・ビット |
| [試験とサニタイザ](docs/notes-tests.md) | fuzz・cmdfuzz・ASan/UBSan が出した 12 件とその直し方 |
| [機械まわりと原典の駆動](docs/notes-machine.md) | 別の機械での続け方、ARM64 への逃がし方、PowerShell の落とし穴、刺された罠 |
| [逆コンパイル出力](docs/notes-decomp.md) | 押さえた番地・クラス構成・リソースの型番・設定の置き場所 |
| [これまでの並び](docs/notes-history.md) | 古い「次にやること」。何がどの順に解けたかの記録 |
