# 引き継ぎ

## いまどこまで

**CAD として一通り使えるところまで来ました。**線を引き、消し、角を作り、
複線を出し、文字を置いて、保存できます。ネイティブ（`jw_port.exe`）と
ブラウザ（<https://yomei-o.github.io/jwcad_win_wasm/>）の両方が動き、
**枠は文字を除いて原典と 0 画素差、ネイティブと WASM の差も 0 画素**です。

窓の大きさを変えても原典どおりに追従し（**別の大きさでも文字の外は
0 画素差**）、ブラウザ版は窓を持たないぶんの**キャプションとメニューバーを
自分で描きます**（こちらも文字と窓ボタンの字を除いて 0 画素差）。

### いま動くコマンド

線・矩形・円・点・連続線・文字／消去（右で図形消去、左 3 回で部分消し。
円弧も。範囲を選んでからならまとめて）・コーナー処理・線伸縮・複線・
属性取得／**範囲選択・複写・移動**／**寸法**／**多角形**／**面取**／
**分割**／**２線**／**中心線**／元に戻る（コマンド単位）／
読取 (R)（端点・点・交点）／**新規**・開く・上書・名前を付けて保存／
拡大縮小・移動・全体表示／コマンドバーはコマンドごとに切り替わり、
その上のボタン（選択確定・選択解除・全選択）が押せ、**数値の欄には
打ち込める**／**線属性のダイアログ**で書込線色・線種を変えられる／
**レイヤとレイヤグループの格子**が押せる（左で表示状態、右で書込）。

**どれも原典に同じ操作をさせて、保存された .jww の座標と突き合わせて
あります。**古いものは小数 3 桁まで（`tests/click_test.c`）、今日入れた
ものは 1e-9 まで、原典が書いたファイルそのものを読んで比べています
（`decomp/res/*.jww` が原典の答え、`tests/*_test.c` がその照合）。
原典の駆動のしかたは「原典を実際に動かして確かめる」の項に。

### 次にやること（この順で）

1. ~~**ネイティブ版に本物のメニューを付ける。**~~ **入りました。**
   `src/main_win32.c` の `build_menu()` が `src/gen/menu.h` の木 176 項目を
   そのまま `CreateMenu` / `CreatePopupMenu` / `AppendMenuW` に流して
   `SetMenu` します（名前は CP932 なので `jw_to_utf16` で UTF-16 に）。
   `WM_COMMAND` は `do_command()` から `app_command(id)` へ ——
   ツールバーのボタンと同じ入口で、ファイルが要るものは同じ
   `app_take_action()` を通ります。`WM_INITMENUPOPUP` でいまのコマンドに
   チェックを付けるのは原典の `ON_UPDATE_COMMAND_UI` と同じ振る舞いです。
   **クライアント領域は 1264×741 のまま**で（`AdjustWindowRectEx` の
   `bMenu` を `TRUE` に）、窓ごと撮って原典と突き合わせても
   **文字の外は 0 画素差**のままでした。
   ショートカットも原典の ACCELERATOR 資源 320 から
   `tools/mkaccel.py` → `src/gen/accel.h` → `CreateAcceleratorTableW` で
   18 個入れてあります（メニューが Ctrl+N と書いているのは資源にそう
   書いてあるからで、効かせるにはこの表が要ります）。

   **ブラウザ版も入りました。**同じ木で、ポップアップを自分で描いて
   押せます（`src/ui.c` の `ui_popup_*`、`app_chrome_press` /
   `app_chrome_move` が入口、`tests/menu_test.c` が検査）。名前を押すと
   開き、もう一度押すと閉じ、外を押すと**閉じるだけで何もしない**、
   サブメニューは親の右に開く、いまのコマンドには印が付く。
   ページ側は何も直していません —— `index.html` はもともと canvas の
   生の座標を `jw_press` / `jw_move` に渡していて、`app_chrome_h()` より
   上なら chrome だと分かるからです。

   **寸法は原典から取りました。**`tools/jwdraw.ps1` の `menu:<文字>,<png>`
   段が `WM_SYSCOMMAND SC_KEYMENU` を投げて、開いた `#32768` を
   `PrintWindow` で撮ります。7 枚の窓の高さを項目数・区切り数と連立させて
   解くと、**項目 22 行・区切り 9 行・枠 3・余白 0** できれいに合いました
   （`ファイル` だけ 2 項目多く出ますが、これは MFC が差し込む
   「最近使ったファイル」です）。位置はフレームのクライアント座標で
   名前の x−8、y=−1。色は本体 `#f9f9f9`・枠 `#e5e5e5`。

   **ポップアップは画素比較しません。**キャプションや窓枠と同じで、原典の
   ものはテーマ付きの窓（角丸・影・別のフォント）なので合わせようがない、
   という既定の扱いです。写したのは寸法だけです。
2. **円弧の 1 画素ずれ。**円の形は GDI と合いました（下記）が、外接矩形の
   作り方が残っています。`天空率表.jww` の残差 2,451 のうち 1,874 が
   「1 画素ずれ」。

   **外接矩形の作り方は分かりました。**`Arc` を呼ぶのは `FUN_004b5090`
   （`CDC::Arc` の下、`FUN_004b50e0` から）で、箱を作っているのは
   `FUN_00421490` です。5 か所すべてが

       FUN_00416040(cx - r, cy - r, cx + 1 + r, cy + 1 + r)

   の形で、**中心 1 点と半径 1 つ**からできています。四隅を別々に丸めて
   いるのではありませんでした。中心は `FUN_004b6d60`、半径は
   `FUN_004b8250` で、

       中心 x = (int)((mx - ox) / mmpp) + bx      ← 切り捨て
       中心 y = by - (int)((my - oy) / mmpp)      ← 切り捨て
       半径   = (int)(r / mmpp + 0.5)             ← 四捨五入

   です。移植の `jw_sx` / `jw_sy` / 円弧の `rp` はもともとこの形でした。

   **違ったのは割り算か掛け算かだけでした。**原典は**画素あたりのミリ**で
   持っていて割ります（`FUN_004b6d60` も `FUN_004b8250` も除算）。移植は
   逆数を持って掛けていました。同じ値ではないので、境界近くの座標では
   1 画素ずれ得ます。**原典と同じ割り算に直しました**（`jw_view.mmpp`）。
   ただし **Test1 の 193・Test7 の 39 は 1 画素も動きませんでした** ——
   この 2 枚では効かない、というところまでです。

   **箱の作り分けも合っていました。**`FUN_00421490` には
   `FUN_00416040` が 2 か所あって、`|振り角| > 6.283185207179586`
   （2π より 1e-7 だけ小さい値）なら `(cx-r, cy-r, cx+r, cy+r)`、
   そうでなければ `+1` の箱です。移植の `odd` の分け方と同じでした。
   ついでに、**半径が 2 未満なら円を描かず点を打ちます**
   （`if (local_e8 < 2)` の枝、`FUN_004b8940` → `FUN_004b7a70`）。
   移植はこれをしていません。

   **正体は位相ではなく「箱」でした（直しました）。**破線の円だけを 6 つ
   置いた図面を作って原典に描かせ、実線版と並べて測ったら、はっきり出ました:

   | | 中心 | 平均半径 |
   |---|---|---|
   | 原典・実線 | 435.50, 278.49 | 64.32 |
   | 移植・実線 | 435.50, 278.50 | 64.31 |
   | 原典・**破線** | 436.54, 279.30 | **65.11** |
   | 移植・破線（当時） | 435.50, 278.52 | 64.32 |

   **原典は、同じ円を実線で描くときと破線で描くときで別の環に置きます。**
   実線は `2r` の箱、破線は**弧の `2r+1` の箱**です（破線の円は弧として
   描かれている、と読むのが自然です）。移植は両方とも `2r` にしていたので、
   環そのものが 1 画素ずれていました —— 刻みの位相をどういじっても直らない
   はずです。`lt != 1` なら `odd = 1` にして、**`天空率表.jww` が
   2,451 → 2,016 画素**、15 枚の合計が 9,887 → **9,452** になりました。

   **潰した仮説**（どれも効きませんでした。同じ道を通らないように）:
   位相を ±3 ずらす（0 が最良）、位相を歩数でなく弧長で進める（悪化）、
   円周に整数個のパターンを収める（ほぼ不変）。

   **残りはまだ環がぴったりではありません。**原典の破線の環は**半径が
   63.9〜66.5 に広がっていて**、単一画素の環ではありません（実線は
   63.7〜64.9）。線種違いの 3 円を重ねても同じでした。刻みごとに別々の
   `Arc` を呼んでいて**端が個別に丸められている**、と読むのがいちばん
   自然です。そこまで合わせるには GDI の `Arc` を短い弧について再現する
   必要があり、大仕事です。

   **ここでも潰した仮説**: 幾何ペン（`ExtCreatePen` の
   `PS_GEOMETRIC|PS_JOIN_BEVEL`）——幅 1 では環は素のペンと同じ画素でした
   （`tools/gdiarc.c` に `style+100` で試せる口を付けてあります）。
   整数画素のずらし（±1 の 4 方向すべて悪化、0 が最良）。

   `JW_ARC_WALK=1` で境界の歩きを書き出せるので、続きはそこから。

   （以前の記録）**残差は線種の位相**`天空率表.jww` の残り 2,451 を
   拡大して原典と並べると、円弧そのものは合っていて、**破線の刻みが
   弧に沿って少しずれています**（`tmp/refs/d09.png` と
   `tmp/d09.out.png` の 320,160 あたり）。1 画素ずれが 1,874／2,451 と
   多いのもこれで説明が付きます。**外接矩形ではありません。**

   **いちばん確からしい筋。**直線については「原典は線種をマスクとして
   かぶせるのではなく、パターンを歩いて**点いているひと続きごとに短い線を
   引く**」ことが分かっていて、移植もそうしています（`src/draw.c` の
   `line`）。**円弧だけは移植が 1 本の連続した画素歩きにマスクを
   かけています**（`phase += 1.0` して `bits_set`）。原典が弧でも同じく
   「ひと続きごとに `Arc` を 1 回」呼んでいるなら、刻みの端の画素は
   別々にラスタライズされるので、いまのような**端だけ 1 画素ずれる**
   差になります。

   確かめ方は 2 つ。`FUN_00421490` の中で弧を描く枝が振り角を刻んで
   何度も呼んでいないかを読むのと、`tools/gdicirc.c` と同じ手で
   **GDI に聞く**——同じ箱・同じ角度で `Arc` を 1 回呼んだ場合と、
   刻みごとに分けて呼んだ場合とで点く画素を書き出して、原典の画面と
   どちらが合うかを見る。

   **道具は用意しました。**`tools/gdiarc.c` が GDI に破線の `Arc` を
   描かせて点く画素を書き出します（`tools/gdicirc.c` と同じで、メモリ
   ビットマップに描くので画面には触りません）。

       gcc -O2 -o tmp/gdiarc.exe tools/gdiarc.c -lgdi32 -lm
       tmp/gdiarc.exe 20 1        # 半径 20、PS_DASH の絵
       tmp/gdiarc.exe 20 0        # 実線。境界そのものの確認用

   **たどり着いた入口。**円弧の描画は
   `FUN_004238c0`（要素 1 つを描く）が線種で二手に分けています ——

       if (*(byte *)(obj + 0x28) < 100) FUN_004390b0(...)   ← 普通の線種
       else                             FUN_004374e0(...)   ← 100 番台（SXF）

   `+0x28` は線種なので、**普通の図面が通るのは `FUN_004390b0`** です。
   `FUN_00421490`（`Arc` を呼ぶところ）はその下にあります。刻みごとに
   呼び直しているかどうかは、この間を読めば分かります。

   （**ペンの枝は本筋ではなさそうです。**`ExtCreatePen(style|0x11000, …)`
   ——`PS_GEOMETRIC|PS_JOIN_BEVEL`——を作る枝は `FUN_004b50e0` にあります
   が、幅の条件が付いていて、条件に掛からなければ素の `Arc` を呼びます。）
2. **包絡処理。**（ハッチ・接線・接円・曲線はこの下に移しました。）

## ハッチ（入っています、0x806a、1線・2線・3線・┬┴┬）

**閉じた領域を見つける話は、原典が自分でやってくれました。**囲いは
**右ボタン**で指定します ——「始めの線・弧をマウス(L)で、**閉鎖連続線・円を
マウス(R)で**指示してください。」—— そして囲いが決まるまで **実行 (1148) は
灰色のまま**です。矩形の 4 辺を左ボタンで 1 本ずつ拾っても灰色のままで
何も描かれず、**どれか 1 辺を右クリックすると輪っか全部**が取れて実行が
効くようになりました。円も右クリック 1 回です。

**引き方は単純で、2 例とも厳密に一致しました。**角度の向きの法線 `n` を
取り、`n·p` が**ピッチの整数倍**になるところに線を引きます。**位相は
領域ではなく 0 に固定**（だから離れた 2 領域のハッチが揃います）。
各線は**その位置での領域の弦そのもの**です。半径 129.88 の円に
角度 45・ピッチ 10 で 26 本、オフセット 190 から −60 まで、どれも
`sqrt(r²−d²)` と 1e-6 まで一致。矩形では 49 本、300 から −180 まで。
**遠い側から順に**出ます（`decomp/res/hatch_*.jww`）。

ピッチは **実寸 (1323) が off の間は紙のミリ**です（原典の初期状態）。
角度の初期値 45、ピッチの初期値 10。

**2線 (1690)・3線 (1691) も入りました。**ピッチごとに 2 本・3 本を
**線間隔だけ離して**引きます。中心は 1線 のときの位置そのままで、
2線 は **±線間隔/2**、3線 は **−線間隔・0・+線間隔**。同じ矩形が
2線 で 98 本（300.5, 299.5, 290.5, 289.5 …）、3線 で 147 本
（301, 300, 299, 291, 290, 289 …）—— **組の中も遠い側から**です
（`decomp/res/hatch_r1690.jww` / `hatch_r1691.jww`）。線間隔の欄は
**ComboBox 1412**、初期値 1（`read:1412` で読みました）。

**┬┴┬ (1692) も入りました —— 煉瓦の目地（馬目地）です。**縦ピッチごとに
領域を横切る線を引き、その間に**横ピッチの半分ごと**の短い縦棒を、
**一段ごとに半目ずらして**置きます。規則は単純で、

* 長い線は **q（ハッチを横切る向きの距離）が縦ピッチの整数倍**のところ。
  ほかのハッチと同じく**原点で位相が決まります**。各線は領域の弦。
* 縦棒は **p（ハッチに沿った距離）が横ピッチの半分の整数倍**のところ。
  その整数を m、段を k（`k*縦` から `(k+1)*縦` まで）とすると、
  **m + k が偶数のときだけ**引きます —— これがずらしの正体です。
* 縦棒は**一段ぶんの長さ**で、線と同じように**領域で切られます**
  （原典は下辺で切れた 0.53 の切れ端を残していました）。
* 順番は、**長い線を q の小さい順**、次に **m が偶数の列を p の大きい順**、
  そのあと **m が奇数の列**。列の中は q の小さい順。

答えは 2 枚取りました —— `hatch_r1692.jww`（バーの初期値 角度 0・縦 3・
横 6、6,467 本）と `hatch_r1692b.jww`（角度 30・縦 20・横 50 を打ち込んで
146 本）。**2 枚目が「原点で決まる」「角度で回る」を押さえます。**

**バーの数値はモードごとに別々です。**1線・2線・3線 は 45・10・1、
┬┴┬・図形 は 0・3・6 で、行き来すると**打ち込んだ値ごと入れ替わります**
（`tools/jwdraw.ps1` の `read:` で確かめました）。移植は `ht_keep[2][3]` に
持って `ht_mode_set()` で入れ替えます。

**実寸 (1323) も入りました。**off の間はピッチは紙のミリですが、
**on にすると図面の単位**になります —— **書込レイヤグループの縮尺で
割る**だけで、1/200 の図面で **ピッチ 2000・実寸 on** は
**ピッチ 10・実寸 off** と**同じ 49 本**になりました
（`decomp/res/hatch_jisun.jww` が `hatch_rect.jww` と一致）。
線間隔も同じように割ります。**コマンドを出入りしても on のまま**です
（欄の数値と同じ）。バーの印は `jw_cmd_bar_check()` を足して出しています。

**基点変 (1147) も入りました。**押すと「基準点を指示して下さい」に変わり、
**次のクリックが模様の起点**になります。線は「その点の横切り距離 ＋
ピッチの整数倍」のところに出て、┬┴┬ の格子は**縦も横もその点から**
数えます（ずらしの m + k もそこから）。**コマンドを出入りすると消えます** ——
入り直したら 0 から数える元の状態に戻りました。

答えは 2 枚（`decomp/res/hatch_base.jww`・`hatch_base92.jww`）で、
基準点には**矩形の角そのもの**を使っています —— 矩形を描いたのと同じ
画素なので、突き合わせで同じ点を指せます。

**範囲選択 (1067) も入りました。**押すと範囲コマンドと同じ枠選択になり
（「範囲選択の始点を…」→「選択範囲の終点を…」）、**選択確定 (1120)** で
**囲んだ閉じた図形が全部まとめて**ハッチの対象になります。白紙に矩形を
2 つ描いて両方を囲ませたら、**2 つ分の弦がひと続きのオフセットで
交互に**出ました（遠い側から順、位相は 0 のまま）——
`decomp/res/hatch_sel.jww`、34 本が 1e-6 まで一致。

そのために**囲いを 1 つから一覧に変えました**（`ht_reg[64]`）。
1 つのオフセットで全部の領域の交点を集めて**まとめて並べ替え**、
偶奇で対にします —— 並んだ 2 つの矩形が 1 回の走査で出るのはこれです。
円も輪も同じ一覧に入ります。

`tests/hatch_test.c` が円と矩形を 1線・2線・3線・┬┴┬・実寸・基点変・
範囲選択 の 10 通りで突き合わせます（1e-6 まで、本数も順番も）。
**残っているのは 図形 (1693) と、左ボタンで 1 本ずつ拾う経路**です。

   **接線の 円→円 は入りました（0x8066）。**2 つの円には共通接線が 4 本
   あって、原典は**指した側で触るもの**を引きます。同じ 2 円に対して
   上/上・下/下・上/下・下/上 と 4 通り指させたら 4 本とも違う線が出て、
   どれも指した側で接していました（`decomp/res/sessen_*.jww`）。線の端は
   接点そのもので、中心から端までの距離は半径と小数 4 桁まで一致します。

   式は、単位法線 `n` について `n・cA − k = sa・rA`、`n・cB − k = sb・rB`。
   引き算して `n・(cB−cA) = sb・rB − sa・rA` なので `n` が（`cB−cA` を軸に
   した鏡像を除いて）決まり、接点は `c − s・r・n`。符号の 4 通りが 4 本で、
   円が重なると内側の 2 本は根号の中が負になって出ません。
   `tests/sessen_test.c` が 4 本とも 1e-6 まで突き合わせます（20 項目）。

   **点→円 も入りました。**円の外の点には接線が 2 本あって、やはり
   **円を指した側に近い接点**のものが出ます（`decomp/res/tensen_*.jww`。
   同じ点から同じ円の上半分を指すと一方、下半分を指すともう一方で、
   左右はそれぞれどちら側にあるかに従いました）。式は `d = P − c`、
   `L = |d|` として、接点は `d` 方向に `r·cos a`、直交方向に `r·sin a`
   （`cos a = r/L`）。`(T−P)·(T−c) = r² − r·L·(r/L) = 0` なので確かに接線です。

   **順番は「点が先、円が後」。**ステータス行は先に「円を指示して
   ください。」と出しますが、**円から先に指すと何も引かれません**。
   点を打ってから円を指すと引かれます。これは実際に両方やって確かめました。

   **角度指定 (1691) と 円上点指定 (1692) も入りました。**どちらも
   **円に接する直線を先に決めて、そのあと 始点・終点 を取ります** ——
   原典は 線 コマンドと同じ言葉で「始点を指示してください」「終点を
   指示してください」と聞きます。

   * **角度指定**: 円 → 始点 → 終点。向きは 角度 の欄（**ComboBox 1412**、
     このモードでだけ現れます）。その向きの接線は 2 本あって、
     **円を指した側**のほうが出ます。
   * **円上点指定**: 円 → 円上の点 → 始点 → 終点。点は**円に引き寄せ**られ
     （中心からその向きに半径ぶん）、そこの接線が直線になります。

   **始点・終点 は直線に下ろした足です。**原典には直線から 500 画素も
   離れたところを 2 回クリックしたのに、線はちょうど 2 つの足の間に
   出ました（`decomp/res/sesang_*.jww`・`sescpt_*.jww`、6 通りとも
   1e-6 まで一致）。長さ の欄 (1411) は空のままで、埋めたときの挙動は
   まだ見ていません。

   モードのボタンは `jw_cmd_bar` が 1689〜1692 を受けます（原典の
   ボタンは押されている見た目になりますが、`mkbars` はこれらを check では
   なく button に分類するので移植はまだ押された絵にしません）。

   **接円（0x8068）の「線 2 本＋半径」も入りました。**クリックは 3 回で、
   1 番目の要素・2 番目の要素・**置く場所**です。原典はここで
   「マウスを移動し、必要な接円位置で左クリックしてください。**【 4 − 1 】**」
   と出すので候補が 4 つあると自分で言っていて、3 回目のクリックに
   いちばん近いものが選ばれます。同じ交差 2 線に対して左・右・上・下に
   置かせたら、半径 10mm（1/200 の図面で 2000）の円が 4 つ出て、中心は
   2 本の二等分線の上に並びました（`decomp/res/sekien_*.jww`）。

   式は、単位法線について `n1・C = k1 + s1・r`、`n2・C = k2 + s2・r` を
   解くだけで、符号の 4 通りが 4 つの円です。半径はバーの値 ÷ 書込
   レイヤグループの縮尺（面取の寸法と同じ）。**平行な 2 線では
   行列式が 0 になって何も出ません**（ちょうど 2r 離れている場合を除き
   解が無いので、これは原典と同じ挙動です —— 平行な 2 線で試したときに
   何も描かれなかったのがそれでした）。

   **円を含む組み合わせも、3 要素指定も入りました。**要素は**線でも円でも
   よく**（ステータス行が「１番目の線・円を」と言っています）、中心の
   条件はどちらでも同じ形です ——

   * 線なら `n・C = k ± r`（線から r だけ離れた 2 本のどちらか）、
   * 円なら `|C − c| = |r₂ ± r|`（外接と内接）。

   符号の 4 通りを解くと、**線+線 は 1 つずつで 4 個、線+円 と 円+円 は
   0〜2 個ずつ**。原典は候補の数を自分で言います —— 交差 2 線で
   **【 4 − 1 】**、線と円で **【 2 − 2 】**、2 円で **【 6 − 1 】**。
   移植の `sek_places()` がちょうどその数を出します。3 回目のクリックに
   いちばん近いものが選ばれるのは前と同じ。

   **半径が空なら 3 要素指定**です。3 本の線を拾うと**3 つ目を拾った瞬間に**
   描かれ（置く場所のクリックはありません）、出てくるのは**三角形の内接円**。
   **どこを拾っても同じ円**でした —— 同じ 3 線をまるで違うところで 3 通り
   拾っても、線を短く切って内接円から遠ざけても、同じものが出ます。
   3 線に接する円は 4 つあって内接円がいちばん小さいので、移植は
   `n_i・C − s_i r = k_i` の 8 通りを解いて**いちばん小さい正の r** を
   取ります。

   **半径が届かないと原典は 2 つ目の拾いを受け付けません。**130 離れた
   線と円に半径 10 の円は触れないので、何度クリックしてもステータス行が
   「２番目の…」のまま動きませんでした。

   **多重円 (1417) も入りました。**欄に n を入れると**中心を共有する n 個**が
   出ます —— 半径は **r・2r/3・r/3**（n=3 のとき）で、**大きい順**。
   3 要素指定でも同じです（`decomp/res/sekmul_*.jww`）。元に戻る は
   まとめて 1 回で戻ります。

   `tests/sekien_test.c` が 13 通り（2 線 4・線と円 2・2 円 4・3 線 1・
   多重円 2）を突き合わせます。**残りは 円を含む 3 要素・接楕円 (1065)**。
## 曲線（入っています、0x808c、4 モードとも）

点を順に打って **作図実行（バーの 1800）**で、点を通る折れ線として引かれます。
聞いて決めたことが 2 つあって、どちらもぴたり出ました。

**何の曲線か —— 一様ノットの自然 3 次スプライン。**4 点を打たせると、
両端の 2 階微分が 0 で、継ぎ目の接線が一致します。1 区間目の始点の接線は
`(y1−y0) − (2m0+m1)/6` で、自然スプラインの 2 階微分から出る
288.6297 と 1e-8 まで同じでした。**Catmull-Rom ではありません** ——
それなら最初の内部点の接線は 0 になるはずで、原典のは −57.73 です。

**どこで刻むか —— 等間隔ではありません。**分割数 n のとき、パラメータの
刻みは **0.58, 1, 1, …, 1, 0.58** の比です（両端だけ 0.58 倍）。つまり
中の刻みが `1/(n − 0.84)`、端が その 0.58 倍。この 0.58 は
**分割数 3・4・7・10 のどれでもちょうど 0.58** でした
（`decomp/res/curve_n*.jww`）。どこから来る数字かは分かりません。

分割数の初期値は **7**。要素は 1 区間につき n 本の `CDataSen` で、
4 点なら 3n 本。`tests/curve_test.c` が 4 通りとも 1e-6 まで突き合わせます。

**ベジェ曲線も入りました（1692）。**打った点**全部**を制御点とする
**次数（点数−1）のベジェ曲線**です。4 点なら 3 次、5 点なら 4 次で、
どちらも 1e-8 まで合いました（`decomp/res/bezier_*.jww`）。スプラインと
違って**中間の点は通りません**（通るのは両端だけ）。

**刻み方はスプラインと別で、こちらは一様です。**頂点の数が
**（点数−1）× 分割数**、つまり線分は**その −1 本**。分割数 3・7・10 で
8・20・29 本、5 点・分割数 7 で 27 本 —— どれも `(N−1)·n − 1` でした。
（スプラインは「線分が (N−1)·n 本」なので 1 本ぶん数え方が違います。）
実装は二項係数を使わず de Casteljau です。

**サイン曲線 (1689) と ２次曲線 (1690) も入りました。**この 2 つが
前に何も描かなかったのは**入力の形が違う**からでした —— 点を並べるのでは
なく、**まず基準線を拾い**ます（ステータス行が
「基準線を指示してください。」と言います。`read:59393`）。段は

* **サイン**: 基準線 → 原点 → 振幅（頂点）の幅の点 → １サイクル点 →
  始点 → 終点
* **２次**: 基準線 → 原点 → 中間点 → 始点 → 終点

で、**作図実行は要りません**（終点を打った瞬間に描かれます）。

拾った線に沿う距離 `s` と、横切る距離 `h` を、**原点から**測ります。
**原点は線に下ろした足ではなく、打った点そのもの**です —— 線から
20 画素ずらして打たせたら、曲線はその点を通る（線と平行な）軸のまわりに
出ました。式は

* **サイン**: `h = A sin(2π s / L)`。`A` は振幅点の `h`、`L` は
  １サイクル点の `s`。
* **２次**: `h = a s²`、`a = 中間点の h / (中間点の s)²`。

**頂点は原点を起点とする格子の上**にあり、曲線は**始点と終点の `s` で
切られます**（だから両端だけ短い）。刻み幅だけが 2 つで違います:

* **サイン**: `L / (2 × 分割数)` —— 1 波が n ではなく **2n 片**。
* **２次**: `(終点の s − 始点の s) / 分割数` —— **描く範囲**を割るのに、
  数えるのは**原点から**。始点が 2.13 刻み目のところにある例で、
  原典は次の頂点を**ちょうど 3 刻み目**に置きました。

拾った線がどちら向きに入っているかは**関係ありません** —— 向きが
ひっくり返ると `s` と `h` が同時にひっくり返り、式はそのままです。

答えは 6 枚（`decomp/res/curve_sin_*.jww`・`curve_q_*.jww`）で、
**斜めの基準線・線から外した原点・格子に載っていない始終点**を含めて
1e-6 まで一致します。この 2 つは点を並べる形ではないので、クリックの
画素を `tests/curve_test.c` にも書いておき、**原典が描いた基準線の
2 端**から図面の単位に直しています（同じ 2 クリックで引いた線なので、
縮尺と原点がそれで決まります）。

連結線指定 (1068)・始終点連続処理 (1323) は未着手です。

3. ~~**属性変更（32952）。**~~ **入りました**（下の項）。
4. **複写・移動の細かいところ。****倍率・回転角・反転・基点変更は
   入りました**（下の項）。残りは 切取り選択・範囲外選択・前範囲・
   属性選択 と、確定後のバーの 任意方向・マウス倍率・作図属性。
5. **寸法の任意角。**いまは 0 度と 90 度だけ（バーの 0ﾟ/90ﾟ）。
6. **文字の書式（文字種）を選べるようにする。**バーの 1843 は押しても
   ダイアログが出ません（下記）。
7. **メニュー**（いまは絵だけで開きません）。

検査は `sh tools/check.sh` 一本で回ります（**442 項目**）。中身は、
15 枚の解析と書き戻しのバイト一致、ピック 15 枚、読取 7 場面、
クリック 124 項目、線属性ダイアログの絵、寸法・多角形・面取・分割・
２線・中心線それぞれの原典との突き合わせ、レイヤの格子、範囲選択と
複写・移動、新規図面の保存、**新規から保存までひと続きの作業**、
枠の画素比較、ネイティブ対 WASM、作図の採点。

最後に通した値:

| | |
|---|---|
| 枠（`docs/ref_start.png`） | 4,076 画素差、**文字の外は 0** |
| ネイティブ対 WASM | **0 画素** |
| Test1 / Test7 の作図 | 文字の外で 193 / 23 画素 |
| 15 枚の作図の合計 | **4,976 画素**（下の表） |
| 線属性ダイアログ | 文字と窓枠の外で 18 画素 |
| 別の大きさの枠（1484x841） | 4,086 画素差、**文字の外は 0** |
| キャプションとメニュー | **文字と窓ボタンの字の外は 0** |
| 検査項目 | 442 すべて ok |

| | |
|---|---|
| 済 | インストーラの展開（Inno Setup 6.4.2、`/VERYSILENT /DIR=`） |
| 済 | PE の構成、インポート 22 DLL / 676 関数（`tools/peinfo.py`） |
| 済 | リソース全部 —— メニュー 3・ダイアログ 108・文字列 1,598・ツールバー 183・ビットマップ 469（`tools/rsrc.py`） |
| 済 | RTTI からクラス 714・vtable 676・継承関係（`tools/rtti.py`） |
| 済 | Ghidra 自動解析（ビルドマシンで 342 秒）、関数 26,061 の棚卸し |
| 済 | 原典の基準画像（`tools/shot.ps1`、`docs/ref_*.png`） |
| 済 | 逆コンパイル 25,983 関数 / 99.8% 成功 / 26 MB。クラスごとに仕分け済み |
| 済 | `CJw_winDoc::Serialize` と `CData*::Serialize` 8 本の位置 |
| 済 | 画素一致の正解を **Direct2D オフ（GDI）**に決定。基準画像も取り直し済み |
| 済 | 基準は**クライアント領域** 1264×741（`docs/ref_start.png`）。枠と画布の座標を実測 |
| 済 | 左右ツールバーのビットマップの担当を特定（下記） |
| 済 | 枠 —— 文字を除いて原典と**差分 0 画素**。ネイティブと WASM も差 0 |
| 済 | `.jww` の形 —— 同梱 15 枚すべて末尾ぴったり（`tools/jww.py`、`tools/jwwcheck.sh`） |
| 済 | `.jww` の読み取りを C に移した（`src/jww.c`）。ネイティブとブラウザ共用 |
| 済 | 線・円弧・点・ソリッドを線色と線種つきで描く。図面は原典と 97.6% 一致 |
| 済 | 文字。字形は東雲フォント（Public Domain）。位置と大きさと色は原典どおり |
| 済 | レイヤの表示・非表示（要素の id は上位 4 ビットがグループ、下位がレイヤ） |
| 済 | 線種の刻みは**用紙のミリ**で進む（1 ビット = 0.625 mm）。画素で進めると 1% ずれる |
| 済 | レイヤバーの印（どのレイヤに要素があるか、書込レイヤ）。原典と**完全一致** |
| 済 | ステータス行の表示、拡大縮小と移動（ホイール・ドラッグ・Home） |
| 済 | 残差の見方 —— `tools/cmp.py --near` が「1 画素ずれ」と「別の場所」を分ける |
| 済 | 用紙は**番号**（0 が A-0）。最後の 2 つの倍精度を角と読んでいたのは誤り |
| 済 | ソリッドの塗り。`tools/refshots.sh` / `scoreall.sh` で 15 枚まとめて採点 |
| 済 | 15 枚ぶん前面で撮って採点（`tools/refshots.sh` → `tools/scoreall.sh`） |
| 済 | **表示のみレイヤ（状態 1）は灰色**（`0xc0c0c0`、ペン 9）で描く |
| 済 | **画面では線幅を使わない**（線幅は印刷用） |
| 済 | ブロック定義（2 本目のリスト）は描かない |
| 済 | 可視判定は `FUN_0043b460`。0=非表示 / 1=表示のみ（灰色）/ 3=通常。
        グループとレイヤの状態を 0・1・3 に畳んで AND、さらに要素の
        `+0x44` のビット 0 が立つと 1 に落ちる |
| 済 | **座標変換は `FUN_004b6d60` そのもの**（下記）。画布は白い矩形ちょうど |
| 済 | **線のラスタライズは GDI と同一**（実機の GDI で確かめた） |
| 済 | **円弧は GDI の Arc**。全円は 2r の枠、部分円弧は 2r+1 の枠 |
| 済 | **線種は線に合わせて伸縮**し、破線は線分ごとに引く |
| 済 | 端点の並べ替えと切り取りの場合分け三つ（`FUN_004280f0`） |
| 済 | 基準環境の復元（`tools/refenv.sh`）と撮り直し（`-Tries`） |
| 済 | **ボタンが効く**。押下状態・コマンド切り替え・線・点・円・元に戻る（下記） |
| 済 | ボタン → コマンド ID（`tools/mkcmd.py`）、コマンド → クラス（`tools/cmdclass.py`） |
| 済 | MFC のメッセージマップを取り出す（`tools/msgmap.py`） |
| 済 | **保存**（`src/jwwrite.c`）。15 枚を読んで書き戻してバイト単位で一致 |
| 済 | 新規・開く・上書・名前を付けて保存・元に戻るが効く |
| 済 | **矩形**（4 本の線。順序は原典に描かせて確かめた、下記） |
| 済 | **連続線**（n 回クリックで n−2 本。最後の 1 本は仮表示のまま、下記） |
| 済 | **元に戻るはコマンド単位**。矩形は 4 本まとめて戻る（原典で確認） |
| 済 | **原典自身の保存形式は v700**。CString が UTF-16、末尾に long が 1 つ増える |
| 済 | CP932 ↔ UTF-16（`tools/mkcp932.py` → `src/gen/cp932.h`、`src/cp932.c`） |
| 済 | **拾い上げ（ピック）**（`src/pick.c`。許容 10 画素、クラスごとの距離） |
| 済 | **消去**。右クリックで図形消去、左クリック 3 回で部分消し |
| 済 | 線・円弧・点・文字・ソリッドすべて拾える（`tests/pick_test.exe`） |
| 済 | **読取（(R)Read）**。端点・点・交点に吸着（`tests/read_test.exe`） |
| 済 | **水平・垂直**。線コマンド中に 線 を押すと反転（下記）。チェックも描く |
| 済 | **コーナー処理**（2 本を交点まで伸ばす／切る。原典と座標一致） |
| 済 | **線伸縮**（端点を指示点へ。動くのは指示点に近い方の端） |
| 済 | **複線**（拾う→方向→確定の 3 クリック。複製は書込ペン・書込レイヤ） |
| 済 | **属性取得**。ペンとレイヤを図形から取る（これで線種・線色が変えられる） |
| 済 | **書込線色・線種は文書に入っていない**（長年の宿題の答え。下記） |
| 済 | **円弧の部分消し**（角度だけで切る。全円は 1 本、円弧は 2 本に） |
| 済 | 読取が**線×円弧・円×円の交点**も拾う |
| 次 | 複写・移動（範囲選択の確定後が未解明、下記） |
| 済 | **コマンドバーがコマンドで切り替わる**（25 本、実機から読み出し。下記） |
| 次 | 円弧の境界画素（GDI の楕円そのものはまだ再現できていない） |
| 次 | 木造平面例.jww だけ原典が目盛（9 mm）を打つ。条件が未特定 |
| 済 | 円弧の掃引角は**負もある**（時計回り）。0 のときだけ真円 |
| 済 | 基準画像は**描き終わってから**撮る（`-Repaint -Cmd 32835`、下記） |
| 済 | ビューの当てはめを 15 枚で総当たり較正（`tools/calibrate.c`） |
| 次 | `Ａマンション立面例.jww` だけ原典が 613 要素中 111 個しか描かない（下記） |
| 次 | 点線の位相、寸法（`CDataSunpou`）とブロックの読み取り |

## これまでに解けたこと（経緯つき）

1. ~~**原典が一部の要素を描かない図面が 3 枚ある。**~~ **これは間違いでした。**
   撮り切れていなかっただけです。原典は図面を開いたときの描画を途中で
   止めることがあり、待っても再開しません。`tools/shot.ps1` に
   `-Repaint`（非表示・再表示）と `-Cmd 32835`（メニューの「全体再表示」）を
   足したら、15 枚中 14 枚が最後まで描かれるようになりました。

   | | 前（途中経過） | 後（描き終わり） |
   |---|---:|---:|
   | `Test3.jww` | 3,071 | 15,251 |
   | `Test6.jww` | 8,903 | 18,213 |
   | `サンプル.jww` | 3,580 | 11,347 |
   | `木造平面例.jww` | 5,341 | 13,613 |
   | `Ａマンション25d.jww` | 2,483 | 11,716 |

2. ~~**`Ａマンション立面例.jww` だけは本当に一部しか描かれません。**~~
   **これも撮り方でした。** 原典の描画には当たり外れがあって、同じ図面・
   同じレジストリでも全部描けるときと途中で止まるときがあります。
   `tools/shot.ps1 -Tries 3` で三回撮って一番インクの多いものを残すように
   したら、4,132 画素だったのが 15,478 画素（こちらの描画は 15,282）に
   なり、**作図領域の不一致は 0 画素**になりました。

   同じ理由で Test2・Test4・Test5 の基準画像も撮り直しになりました
   （6,572 → 19,814、4,803 → 5,869、1,991 → 7,490）。

3. **画布に残るずれ。** 採点は `sh tools/scoreall.sh`（作図領域と枠を
   別々に数えます）。いま作図領域 **10,920 画素**・枠 324 画素。

   | | 作図領域 | 1 画素ずれ | |
   |---|---:|---:|---|
   | `敷地図.jww` | **0** | 0 | 枠も 0 |
   | `Ａマンション立面例.jww` | 27 | 0 | |
   | `Ａマンション25d.jww` | 34 | 0 | |
   | `Test7.jww` | 39 | 14 | |
   | `Test5.jww` | 55 | 0 | |
   | `Test4.jww` | 175 | 42 | |
   | `Test1.jww` | 193 | 82 | |
   | `Test2.jww` | 424 | 191 | |
   | `サンプル.jww` | 484 | 259 | |
   | `Test6.jww` | 700 | 494 | |
   | `日影図.jww` | 843 | 520 | |
   | `Test3.jww` | 916 | 195 | |
   | `木造平面例.jww` | 929 | 67 | ほぼ全部が目盛（下記） |
   | `天空率表.jww` | 2,880 | 2,328 | ほぼ全部が円弧 |
   | `Ａマンション平面例.jww` | 3,221 | 385 | 下記 |

   （円の表を入れる前は 10,920 画素でした。下記）

   **この表は 2026-09-16 の実測です。**前の版には 7,305 画素と書いて
   ありましたが再現しませんでした。今回の変更が原因ではありません
   （`src/jww.c` の変更を戻して採点しても同じ 55 画素でした）。
   `Ａマンション平面例.jww` の 3,221 は 2,836 が「別の場所」で、
   1 画素ずれの形ではありません。原典の描画には当たり外れがあるので
   （上の 2 参照）、疑う前にまず `tools/refshots.sh` で撮り直すこと。

   残っているのは大きく二つ。

   ~~**円弧の境界画素。**~~ **表にしました。**GDI の円は教科書の中点円では
   ありません（半径 5 で教科書は (0,5)(1,5)(2,5)(3,4)、GDI は
   (0,5)(1,5)(2,4)(3,4)。半径の二乗にどんな定数を足しても再現できず、
   半径 16・18・19・50・80・120 では成り立つ定数が存在しません）。
   そこで **GDI 自身に描かせて焼き込みました**:

       gcc -O2 -o tmp/gdicirc.exe tools/gdicirc.c -lgdi32
       tmp/gdicirc.exe > decomp/res/circles.txt
       python tools/mkcirc.py            # → src/gen/circle.h

   メモリビットマップに描くだけなのでウィンドウも画面も使いません。
   半径 256 まで、四分円を (0,r)→(r,0) の歩きとして 2 ビット/歩
   （x へ・y へ・斜め）で持ち、`src/draw.c` が残り 3 つを鏡映します。
   **円そのものの不一致は 20〜25% から 0.3〜0.45% になりました**
   （残るのは GDI の円が厳密には四回対称でないぶん）。
   半径 256 を超えるときは従来の中点楕円に戻ります。

   **ただし図面の採点は 10,920 → 10,434 画素にしか下がりませんでした。**
   `天空率表.jww` は 2,880 → 2,451 ですが、その 1,874 が「1 画素ずれ」で、
   円の形ではなく**外接矩形の作り方**が残っているようです。移植は中心画素と
   半径を別々に丸めていますが、原典が四隅を別々に丸めているなら 1 画素
   ずれます。`Arc` を呼ぶのは `FUN_004b5090`（`all_004894cd.c:25452`）で、
   その引数をどこで作っているかを追うのが次の一手。
   なお GDI の `Arc` は同じ箱の `Ellipse` とも 2.6% 違う（角度から描き
   始めるため）ので、そこまで合わせるには角度も含む表が要ります。

   **目盛（解けました、入っています）。**前の引き継ぎは「間隔は 15 枚とも
   9.0」と書いていましたが、それは別の場所の 9.0 でした。**図面自身が持つ
   目盛の設定は `settings` ブロックの末尾の倍精度 5 つ**で、移植はそこを
   読み飛ばしていました:

       最小画素数, 横の間隔, 縦の間隔, 原点 x, 原点 y

   15 枚とも最小画素数は 15。**間隔は `木造平面例.jww` だけ 9mm で、
   残りの 14 枚は 5mm** です。画面上では

   * 木造平面例（A-4、3.267 画素/mm）: 9 × 3.267 = **29.4 ≥ 15** → 描く
   * A-3 の 6 枚: 5 × 2.310 = 11.5 < 15 → 描かない
   * A-2 の 5 枚: 5 × 1.633 = 8.2、A-1: 5.8、A-0: 4.1 → 描かない

   この 1 つの規則で 15 枚すべて説明が付きます。点は**用紙ではなく作図領域
   いっぱい**に打たれます（用紙の端より 2 列ぶん外まで出ていました）。色は
   線色 2（黒）。描くのは全部の要素より**先**です。`木造平面例.jww` が
   **870 → 27 画素**になりました。

## 別の機械で続けるとき

一度やりました（ARM の上のエミュレーションから Intel の Windows 11 へ）。
**基準画像は撮り直さなくてよい**ことが分かっています —— `docs/ref_start.png`・
`docs/ref_test1.png`・`docs/ref_zoku.png` を撮り直して、いずれも文字と
テーマの枠を除いて **0 画素差**でした。GDI のラスタライズも Windows の
フォントも機械をまたいで同じ画素を出します。

条件は一つだけです。

* **画面の拡大縮小を 100% にすること。** `Jw_win.exe` はマニフェストに
  `<dpiAware>true</dpiAware>` を持つ system DPI aware なアプリなので、
  150% のままだと 144dpi で自分を描き直し、字も枠も基準と別物になります。
  `tools/shot.ps1` は窓を 1280×800 にするので作業領域も 800 行以上要ります。

作り直しが要るのは、リポジトリに入らないものだけです。
`sh tools/gen.sh` が全部やります（後半は原典を動かすので 20 分ほど）。
Ghidra は `tools/decomp_box.ps1`（`GHIDRA_HOME` / `JAVA_HOME` /
`GHIDRA_HEADLESS_MAXMEM` を環境から見ます）。8 スレッド 24 GB の機械で
6 シャード・約 13 分、**25,983 関数 / 99.8% / 26 MB** —— ビルドマシンで
出た数と同じでした。

## ステータス行は読めます（写真は要りません）

**`read:59393` で原典が今なにを聞いているかが読めます。**ステータス行は
フレームのただの子ウィンドウで、`WM_GETTEXT` がそのまま文字を返します
（`GetWindowText` は他プロセスでは空です —— `tools/jwdraw.ps1` の `TxtMsg`
がその回避です）。

```
read 59393: [ 円を指示してください。     ]
read 59393: [ 円上点を指示してください。   (L)free (R)Read]
read 59393: [ 始点を指示してください  (L)free  (R)Read]
read 59393: [ ◆　　終点を指示してください  (L)free  (R)Read]
```

これで**コマンドの段取りが実測できます** —— 接線の 角度指定・円上点指定 は
この 4 行そのままで分かりました。以前はここを画面写真で確かめていました。

## 原典を駆動するときの落とし穴（PowerShell）

`tools/jwdraw.ps1` を書き直したときに全部踏みました。どれも「エラーが
出ないまま静かに間違う」ので、疑う順に並べておきます。

* **`saveas:` は既存のファイルを上書きしません。**「上書きしますか」の
  確認が出て、そこで止まります。ファイルは前のまま残るので、存在を見る
  だけの確認は通ってしまい、**何度やり直しても前回の結果が返る**という
  形で出ます。いまは保存の前に消しています。
* **`c`（実カーソルを動かしてからクリック）だと拾いません。**
  素の `WM_LBUTTONDOWN` は通ります。ステータス行を見ると、`c` の拾いでは
  「基準線を指示してください。」のまま動かず、次の素のクリックで進みます。
  RESUME の表は `c` で書いてありましたが、**要らないどころか邪魔**です。
  段がどこまで進んだかはステータス行を撮るのがいちばん早い
  （`shot:` してその下 19 行を見る）。
* **`.ps1` は UTF-8 の BOM を付けること。** PowerShell 5.1 は BOM の無い
  `.ps1` を CP932 として読むので、日本語のコメントが化けて**次の行を
  飲み込みます**。`# 名前を付けて保存` の次の行が実行されませんでした。
* **`LP` という名前の関数は作れません。** PowerShell に `lp`
  （`Out-Printer`）のエイリアスがあり、エイリアスが関数より優先されます。
  クリックが全部プリンタに行きました。
* **`GetWindowText` は他プロセスのコントロールの文字を返しません。**
  Edit も ComboBox も空で返ります。`WM_GETTEXT` を送れば読めます
  （`read:<id>` 段）。`bars.txt` のコンボが空なのはこれが理由で、
  Static と Button の文字はウィンドウのタイトルなので普通に読めます。
* **`[void](...)` は中の `Write-Output` ごと捨てます。** 行は `-Out` の
  ファイルには入るのに標準出力には出ない、という形で出ます。
* **関数から `[IntPtr]` を返すのは危うい。** 本体が何か出力していると
  配列になって返り、`-eq [IntPtr]::Zero` が偽になって次で落ちます。
  ハンドルは `$script:` の変数で受けるほうが安全です。
* **拾いは 4 回に 3 回くらいしか通りません。** 同じレジストリ・同じ
  クリックでも、コマンドが 1 段目のまま何も描かないことがあります。
  `tools/refanswers.sh` は図面ごとに対応する検査を回して、食い違えば
  引き直します。

## 保存された「選択」は選択ではありません（直しました）

**移植は、ファイルに入っていた選択ビットをそのまま選択色で描いていました。**
`天空率表.jww` は 6,475 要素のうち **245 個**が `+0x44` のビット 1 を
立てた状態で配布されていて、移植はその 245 個を ff00ff（Pen/Color10）で
描いていました。原典は**自分のペンの色で描きます**。

原典に開かせて名前を付けて保存させ、ビットを数え直して確かめました ——
**245 個のまま、1 つも変わりません。**つまり原典はこのビットを

* 読み込んでも**表示には使わず**（選択はセッションの持ち物）、
* 保存のときに**そのまま書き戻す**（データとして運ぶ）

という扱いをしています。前の引き継ぎの「選択は要素自身の +0x44 のビット 1」
は、**保存したときに何が選ばれていたかが残る**という意味では正しいのですが、
**読み込んだときにそれが選択されている**という意味ではありませんでした。

移植は `jw_obj` に `sel`（ファイルに入らない）を足して、いま選んでいるかは
そちらで持つようにしました。ビット 1 は選ぶ・外すのたびに一緒に更新するので、
**移植が保存した図面も原典と同じことを言います**（書き戻しのバイト一致も
そのまま）。

**効きました。**15 枚の採点の合計が **19,219 → 10,333 画素**（さらにソリッドの端を直して 9,887）。
`天空率表.jww` は 5,752 → **2,451（うち 1,874 が 1 画素ずれ）**で、
前の引き継ぎに書いてある数字とぴたり同じになりました。

## 補助線の刻みが 1 画素ずれていた（直しました）

線種を 1 本ずつ引いただけの図面を作って原典に描かせ、突き合わせました
（`tmp/lines.jww` を `tools/jwdraw.ps1` の `shot:` で撮るだけなので、
画面を占有しません）。**線種 2〜8 は画素まで一致**、**線種 9（補助線）
だけが 1 画素ずれ**ていました:

```
  原典: ......#...#...#...#...#...#...
  移植: .....#...#...#...#...#...#...
```

`src/draw.c` の `LTYPE[9]` を `0x22222222` から `0x44444444` に
（パターンを 1 ビット送るだけ）。この 1 ビットで:

| | 前 | 後 |
|---|---:|---:|
| Test6 | 700 | **219** |
| サンプル | 435 | **252** |
| 天空率表 | 2,016 | **1,487** |
| Ａマンション平面例 | 568 | **504** |
| Test7 | 39 | **23** |
| **15 枚の合計** | **6,248** | **4,976** |

補助線は画面にしか出ない線なので、どの図面にも薄く効いていたということです。

## 点は二種類ある（入っています）

`CDataTen` の**末尾の long が 1 なら 実点で、小さな輪が付きます**。0 の
仮点は 1 画素のままです。移植はどちらも 1 画素にしていました。

輪はどこに出ても同じ 11 画素で、**左右対称ではありません**（`Test3.jww`
の、周りに何も無い点 2 つを読み取って一致を確認）:

```
 dy\dx  -2  -1   0   1   2
  -2     .   #   #   .   .
  -1     #   .   .   #   .
   0     #   .   .   .   #
   1     #   .   .   .   #
   2     .   #   #   #   .
```

GDI の半径 2 の円（`tools/gdiarc.exe 2 0`）は上下とも 3 画素の左右対称な
輪なので、これとは別物です。原典が円を 2 回の `Arc` で描くこと（`FUN_00421490`）
と関係がありそうですが、形そのものは実測どおりに焼いてあります。

`Test3.jww` が 902 → 825、`Test4.jww` が 171 → 94 画素。

## 15 枚の採点（`tools/refshots.sh` → `tools/scoreall.sh`）

| | 残差 | 1 画素ずれ | ほかの場所 | 画布比 |
|---|---:|---:|---:|---:|
| Test1 | 193 | 94 | 99 | 0.025% |
| Test2 | 462 | 196 | 266 | 0.061% |
| Test3 | 294 | 178 | 116 | 0.039% |
| Test4 | 94 | 20 | 74 | 0.012% |
| Test5 | 55 | 0 | 55 | 0.007% |
| Test6 | 219 | 4 | 215 | 0.029% |
| Test7 | 23 | 0 | 23 | 0.003% |
| サンプル | 252 | 44 | 208 | 0.033% |
| 天空率表 | 1,487 | 1,102 | 385 | 0.196% |
| 円地図 | **0** | 0 | 0 | 0.000% |
| 日影図 | 773 | 533 | 240 | 0.102% |
| 木造平面例 | 28 | 8 | 20 | 0.004% |
| Ａマンション25d | 34 | 0 | 34 | 0.004% |
| Ａマンション平面例 | 504 | 319 | 185 | 0.066% |
| Ａマンション立面例 | 27 | 0 | 27 | 0.004% |
| **合計** | **4,445** | 2,498 | 1,947 | |

残りの大口は三つです。

* **`Ａマンション平面例` の 568**（前は 3,221）。この図面だけ `CDataSolid` が
  79 個あります。**ソリッドは要素の順ではなく、先にまとめて塗ります。**
  順番どおりに塗ると、それより前に描かれた線がソリッドの下に埋もれます ——
  原典が黒にしている 1,126 画素と水色にしている 1,086 画素が移植では灰色に
  なっていました。全部先に置いてから線を描くと、**2,775 → 568 画素**。
  **塗りつぶしの端も直しました** ——
  原典は `Polygon` に**ヌルペン**で塗らせていて、GDI は**右端と下端を
  塗りません**（`tools/gdipoly.c` で直接聞きました。(17,4)-(20,50) の
  四角は x 17..19・y 4..49 になります）。移植は両端を含めていたので、
  細い壁ばかりのこの図面では要素の大半が余分でした ——
  79 個で 7,809 画素、原典は 3,818。半開に直して 3,221 → **2,775**。
  残りは壁の輪郭線が 1 画素ずれる話で、下の円弧と同じ「線の位置」の
  問題に見えます。
* **`天空率表` の 2,451**（うち 1,874 が 1 画素ずれ）。円弧の外接矩形の話
  （上の「次にやること 2」）。円弧が 35 本と多い図面です。
* ~~**`木造平面例` の 870**~~ → **27**。目盛の条件が分かって入りました（下記）。

## ペンの太さ（入っています）

**図面はペンごとに太さを持っていて、原典は画面でもそれを使います。**
既定の表は `pen 6 = 2`・`pen 7 = 3`・`pen 8 = 5` で、`Test3.jww` の青い
破線の枠（線色 6・線種 2）は原典では **559 行と 560 行の 2 行**、
**427 列と 428 列の 2 列**に出ます。移植の `pen_wide()` は常に 1 を返して
いたので、そこだけで **533 画素**ずれていました。`Test3` は
**825 → 294 画素**、15 枚の合計は **4,976 → 4,445**。

太さの出どころは `FUN_004db560` です。

```c
if (*(int *)(in_ECX + 0x2c58) == 0)  CreatePen(PS_SOLID, width, colour);
else  ExtCreatePen(style | PS_GEOMETRIC | PS_JOIN_BEVEL, width, &brush, 0, 0);
```

**太い線は正方形の判子を線に沿って押したものではありません。**GDI は端を
丸め、しかも偶数の太さでは左右対称ですらありません。`tools/gdiwide.c` が
GDI に直接聞いて `decomp/res/widepen.txt` に書き出し、`tools/mkwide.py` が
`src/gen/wide.h` にします。表は**線を横切る向きのずれ `d` ごとに、線に
沿った開始と終了のずれ**です（`d` は `-(w/2)` から `(w-1)/2`）:

* **太さ 1** は `{0, -1}` —— `LineTo` は最後の点を描きません。同じ表で
  まかなえるので、細い線も太い線も 1 つのループで済みます。
* **偶数の太さ**は余分の画素が**水平線なら上・垂直線なら左**に出ます。
* **水平線のいちばん外側の行は 1 画素遅れて始まります**（`{+1, 0}`）。
  移植が最初これを落として、原典が 2 画素の破片を並べるところに 3 画素を
  並べ、152 画素余計に塗っていました。
* **太さ 3 以上は端が丸い**ので、線の両端に少しはみ出します
  （太さ 5 なら中の 3 行が ±2、外の 2 行が ±1）。

移植側は `stroke()` が**ずれごとに 1 本ずつ**走らせます
（`stroke_at()`）。破線は 1 つの run が 1 本の `LineTo` なので、run ごとに
端の丸めが付きます —— そこが表と合っていないと、破線の数だけ誤差が出ます。
画素単位で歩く経路（点線・楕円の弦）には run がないので `wide_dot()` が
真ん中の形だけを押します。

残りは**線が交わる角の 6 画素**だけです（GDI の継ぎ目の規則）。

## 包絡処理（まだです、0x804e `CZukeiHouraku`）—— 分かっていること

**範囲を枠で囲む**コマンドです。ステータス行が段取りを教えてくれます:

```
包絡範囲の始点指示を指示して下さい
包絡範囲の終点を指示して下さい　(L)包絡処理　(R)範囲内消去  (Shift+L) (Ｌ←)中間消去
```

バーは **実線 (1338)・点線 (1339)・鎖線 (1340)・補助線 (1341)** の 4 つの
チェックと **建具線端点と包絡 (1357)**。初期は 実線 だけ入。

**枠が線を横切っているときは、ちゃんと「包絡」します。**白紙
（`decomp/res/new.jww`）に矩形を 2 つ描いて試しました:

* **十字に交わる 2 本の壁**を、交点のあたりだけ囲む → **12 本**の
  十字の輪郭。中の 4 本が消え、通し線は**相手の壁の線のところで**
  切られます（枠の縁ではありません）。
* **L 字に重なる 2 つの矩形**の角だけ囲む → **6 本**の L 字の輪郭。
  入り隅もちゃんと残ります。枠の中に丸ごと入っていた 2 本
  （短い辺と、他と重なる辺）は**吸収**されました。
* **離れた 2 つの正方形**を**帯で横切る** → **8 本**。2 つが
  **1 つの図形につながり**ました —— 下の正方形の底辺が相手の右端まで、
  上の正方形の上辺が相手の左端まで伸びて、Z 字形の閉じた輪郭に。

**枠が全部を丸ごと含むと、外接矩形になります。**十字でも L 字でも
離れた 2 つでも、囲んだもの全部の**バウンディングボックス 4 本**だけが
残りました。

**この 2 つは、こう読むと同じ規則になります:**

> **枠を横切っている線はそのまま**（外に続くので動かせない）。
> **枠の中に丸ごと入っている線は、自分の向きに伸ばされて**、
> 横切っている線のところまで行く。そのうえで**全体の輪郭**を描き、
> 内側に入った辺は落とす。

* **十字の交点だけ囲んだとき**（8 本とも横切る）: 伸ばすものが無いので、
  2 つの壁の**和の輪郭**そのまま —— 12 本。
* **L 字の角だけ囲んだとき**: 丸ごと入っていた 2 本（短い辺と、
  相手と同じ直線に乗る辺）は伸ばされて**相手に吸収**され、6 本。
* **平行な 2 本の壁を帯で横切っても、何も起きません**（8 本のまま）。
  だから「囲んだ線を総当たりで対にする」ではありません —— それなら
  2 本の壁の間が埋まってしまいます。中に丸ごと入った線があるかどうかが
  効いているようです。
* **離れた 2 つの正方形を帯で横切ったとき**: 横切るのは 4 本の縦線で、
  丸ごと入っているのは**下の正方形の上辺と上の正方形の下辺**。
  これが左右に伸びて**いちばん遠い縦線まで**届き、2 つが 1 つに
  つながって 8 本 —— 実際そう出ました。
* **全部を丸ごと含んだとき**: 横切る線が 1 本も無いので**全部が伸び**、
  行き着く先は図形全体の端 —— つまり**外接矩形** ✓。

**まだ実装していません。**`CZukeiHouraku` 自体は 519 行と小さく、計算は
`FUN_0067f210`（**19,431 バイト・約 1,345 行**、呼び出し元は 1 つ）に
あります。上の読みでどこまで合うかは、斜めの線や円弧を含むところで
確かめる必要があります。

## 文字種のダイアログ（バーの 1843）—— 出せました

**「押してもダイアログが出ない」のは `tools/jwdraw.ps1` の不具合でした。**
`dlg:b<id>` の枝が

```powershell
$byButton = $s -match '^dlg:b'      # ← これが $Matches を書き換える
$id  = [int]$Matches[1]             #    ので id が 0 になっていた
```

と書いてあり、**PowerShell は `-match` のたびに `$Matches` を作り直す**ので、
id を読む前に消えていました。群を先に読むように直したら一発で出ました。
（`btn:1843` は `SendMessage` なのでモーダルの間こちらが止まります。
**ダイアログを開くボタンは `dlg:b` で。**）

中身（406x375）は **任意サイズ (1884)** と **文字種[ 1]〜[10] (1689〜1698)**
のラジオ、幅・高さ・間隔の欄 (1491/1492/1493)、色 No. (2358)、
フォント (1423)、斜体 (2420)、OK (1)・キャンセル (2)。各行の数値は
Static 2504〜2513 に出ていて、この図面では

| | 幅 | 高さ | 間隔 | 色 |
|---|---:|---:|---:|---:|
| 1 | 2.00 | 2.00 | 0.000 | 1 |
| 2 | 2.50 | 2.50 | 0.000 | 1 |
| 3 | 3.00 | 3.00 | 0.500 | 2 |
| 4 | 4.00 | 4.00 | 0.500 | 2 |
| 5 | 5.00 | 5.00 | 0.500 | 3 |
| 6 | 6.00 | 6.00 | 0.500 | 3 |
| 7 | 7.00 | 7.00 | 1.000 | 4 |
| 8 | 8.00 | 8.00 | 1.000 | 4 |
| 9 | 9.00 | 9.00 | 1.000 | 5 |
| 10 | 10.00 | 10.00 | 1.000 | 5 |

**この 10 個は図面が持っています** —— 移植の `jw_drawing.style[10]` が
ヘッダから読んでいるもので、寸法が使う `MOJINO=2` の 2.5/2.5/0・色 1 とも
合います。バーのボタンは今の書式を `Free  W=20 H=20 D=0 (1)` のように
出しています。残るのは**選ばせる UI** で、`cur_style` を差し替えれば
文字はもうその書式で置かれます。

## ボタンの押し方は 2 通り（`btn:` と `pb:`）

**`btn:` は `SendMessage`** なので、**モーダルなダイアログを出すボタンだと
そこで止まります**（文字種の 1843 で丸ごと固まりました —— 原典ごと
kill する羽目に）。**`pb:` は `PostMessage`** で投げるだけなので止まりません。
ハッチの 基点変 のように「押すと次のクリックの意味が変わる」ボタンは
`pb:` で押せば、そのまま次のステップに進めます。ダイアログの中身が
見たいときは `dlg:b<id>,<png>`。

## 原典が保存すると設定の文字は最後に回ります

**原典が書いた `.jww` は、6 つの設定文字（`Draw_BmpTOUKA = 1` など）が
末尾に来ます。**白紙（`decomp/res/new.jww`）はその 6 つだけの図面なので、
そこに描いて保存すると **図形が先・設定文字が後**になります。
「白紙の要素数だけ読み飛ばす」と**線を 6 本取りこぼします** ——
`tests/hatch_test.c` の範囲選択の突き合わせでこれを踏みました。
**クラスで拾う**のが正解です。

## 属性変更（入っています、0x80b8）

**引き金は「範囲を確定してから」ではなく、要素をひとつクリックするだけ**
でした。ステータス行がそう言っています:

```
変更するデータを指示してください。   線・円・実点(L)    文字(R)
```

（前は「範囲を確定してからコマンドを送っても変わらない」と書いてありま
したが、そもそも範囲は使いません。`read:59393` で一行読めば済む話でした。）

クリックした要素は

* **書込レイヤに移り**（`Test5.jww` の 1 本目はレイヤ 0 → **8**。8 は
  この図面で新しい要素が乗るレイヤです）、
* **図面の末尾に移ります** —— 後ろにあったものが 1 つずつ前に詰まり、
  座標はそのまま。だから**重なりの上下が変わります**。
* 線色・線種も書込のものになります。`Test5.jww` の線はもともと
  書込と同じ（この環境の原典は 線色 1・線種 2）だったので、
  保存された答えでは見た目の変化がレイヤだけでした。

バーの印は **線種・文字種変更 (1352)** と **書込みレイヤに変更 (1353)**、
どちらもコマンドに入ると入っています。移植は両方を見て、それぞれの半分を
適用します。

`tests/zokuhen_test.c` が、原典の答え（`decomp/res/zokuhen.jww`）と
**末尾に移ったこと・ほかの順番が変わっていないこと・移った先のレイヤが
新しい要素のレイヤと同じこと**を突き合わせ、移植側は**自分が新しい要素に
与える線色・線種・レイヤ**になることを確かめます（書込線色・線種は
ファイルに入っていないので、そこは環境ごとに違います）。

## 複写・移動の倍率と回転角（入っています）

**選択確定のあとバーが 2 段目に変わり**、そこに **倍率 (ComboBox 1411)** と
**回転角 (1412)** が出ます（ほかに 数値位置 1413・基点変更 1066・反転 1067・
任意方向 1151・マウス倍率 1069・作図属性 1070・角 1071・/ 1072・連続 1064）。
このバーはこれまで読み出していませんでした。

**置かれる位置はこの式ひとつです:**

```
    p' = クリック点 + R(回転角) × 倍率 × (p − 基準点)
```

矩形を 倍率 2・回転角 30 で複写したものと、同じ矩形を 0.5・−45 で移動した
ものが、どちらも小数 4 桁まで一致しました（`decomp/res/copyxf.jww`・
`movexf.jww`）。円は半径に倍率が乗り、開始角と傾きに回転角が足されます。
文字は大きさと間隔に倍率が乗ります（`jw_obj_xform()`）。

**基準点はクリックではなく「選択確定を押したときにカーソルがいた場所」**
です（前からそう書いてありましたが、原典に同じことをさせるには
カーソルを動かす手が要ります）。そのために `tools/jwdraw.ps1` に
**`m<x>,<y>`**（クリックせずにカーソルだけ動かす）を足しました。

**反転 も入りました。**2 段目の **1067**（1 段目の 選択解除 と同じ id が
**反転** に変わります）を押すと

```
基準線を指示してください。   文字方向補正無(L)     有(R)
```

になり、**次のクリックで拾った線に対して鏡像**になります。置くための
クリックはありません。点はその線に対して折り返すだけで、**線の端の順番は
そのまま**でした。円弧は中心を折り返し、**開始角が 2φ −（開始角＋掃引）**、
傾きが **2φ − 傾き**（φ は基準線の角度）になるはずで、そこは構成から
入れてあります（原典での確認はまだ）。

**基点変更 も入りました。**2 段目の **1066**（1 段目の 全選択 と同じ id）を
押すと「基準点を指示して下さい」になり、**次のクリックが新しい基準点**、
その次が複写先です。**カーソルの位置はもう使われません**（ステータス行も
「複写先の点を指示して下さい … X=21,428.571 Y=-11,020.408」と、基準点から
のずれを出します）。

**2 段目でボタンの意味が変わる id が 2 つあります**: **1066** は
全選択 → 基点変更、**1067** は 選択解除 → 反転。移植は `sel_step == 3`
（確定済み）かどうかで振り分けます。どちらも原典では**2 段目でも有効**
なので、`jw_cmd_bar_enabled()` もそう直しました（前は 1066 を
「確定後は無効」にしていて、押しても何も起きませんでした）。

答えは 5 枚 —— 倍率・回転角が `copyxf.jww`・`movexf.jww`、反転が
`flip.jww`（垂直な基準線・複写）と `flipmv.jww`（斜めの基準線・移動）、
基点変更が `basept.jww`。`tests/xform_test.c` が 5 通りとも突き合わせます。

## 刺された罠

**`w64devkit/bin` を PATH の先頭に置いてはいけません。**そこの
`sh.exe`（busybox）が Git Bash の `sh` を隠し、`[ -f /c/prog/... ]` が
**偽**になります —— busybox は MSYS の `/c/...` を知りません。これで
`tools/check.sh` が `emcc not found at /c/prog/emsdk/...`（実際には在る）
で落ちました。各スクリプトは自分で w64devkit を探すので、**PATH も `CC` も
いじらずに `sh tools/check.sh` と打つのが正解**です。


**原典に試させるなら、まず図面を用意すること。**寸法の 3 段目
（寸法の始点）が空の図面ではどう押しても通らず、コマンドの仕様の問題だと
思って長く探しました。**読み取れる点がないと受け取らない**段だったので、
線を 1 本引いてからならすぐ通りました。中心線も同じで、線 2 本を先に
描いてから試すべきでした。原典を動かして確かめるときは、**まず何か描く**。

**バーの数値欄に値を入れるには本物の `WM_CHAR` が要る。**`WM_SETTEXT` で
コントロールの文字を書き換えても、コマンドは自分の持っている値で描きます
（多角形で、寸法を空にしても五角形が同じ大きさで出続けました）。
`tmp/jwdraw.ps1` の `ch:<id>,<文字>` が 1 文字ずつ送ります。

**PowerShell の変数名は大文字小文字を区別しない。** `param([long]$Lo)` の下で
`$lo = $Lo + $i * $step` と書くと**同じ変数**です。分割して逆コンパイルする
`tools/decomp_box.ps1` で、2 番目以降のシャードが「開始 > 終了」になって
黙って 0 関数を出しました。ログは正常、`DONE` も出るので気づきにくいです。
いまは `$rlo` / `$rhi` にしてあります。

**`PrintWindow` では図面が撮れない。** 枠とメニューは返ってきますが、
Direct2D のスワップチェーンは見えないので**作図領域が真っ白**になります。
`tools/shot.ps1 -Screen` はデスクトップから画素を読みます。

**撮影は画面を占有するので最小限に。** `-Screen` は前面に持ってきて
デスクトップから画素を読むので、その間は機械が使えません。15 枚撮るのに
20 分ほどかかります。まとめて一度だけ撮ること。

**原典の描画には当たり外れがある。** 同じ図面・同じレジストリでも、全部
描けるときと途中で止まるときがあります。Ａマンション25d.jww は 11,716 と
2,483 を行き来しました。`tools/shot.ps1 -Tries 3` で三回撮ってインクの
一番多いものを残します（インクは減ることはあっても増えないので）。
「原典が一部の要素を描かない図面がある」という結論に二度たどり着いて
二度とも間違いでした。疑う前にまず撮り直すこと。

**画面の大きさが変わると原典のツールバー配置が崩れる。** MFC は
`ToolBar-Summary\ScreenCX/CY` に配置を保存したときの画面寸法を入れておき、
今の画面と違うと保存した配置を捨てます。レジストリを入れ直しても直らないので
たちが悪い。`sh tools/refenv.sh` が今の画面寸法を書き込んで直します。

**原典は描画を途中で止める。** 図面を開いたときの描画が段階的で、
途中で止まったまま戻ってきません。しかも待っても再開しないので、
「画面が変わらなくなるまで待つ」では途中経過を撮ってしまいます。
`-Repaint`（非表示・再表示）と `-Cmd 32835`（全体再表示）を送ってから、
**プロセスの CPU 時間が伸びなくなるまで**待つこと。画面を見て待つと、
描画の合間の休みで抜けてしまいます（文字を描く前で止まる）。

**`-Repaint` を最小化・復元でやってはいけない。** MFC がツールバーの
配置を `HKCU` に書き戻すので、以降の基準画像の枠が全部変わります。
一度やって 15 枚撮り直しました。非表示（`SW_HIDE`）・再表示
（`SW_SHOWNOACTIVATE`）なら枠はそのままです。壊したときは
`reg delete "HKCU\Software\Jw_cad\jw_win" /f` してから
`reg import tools/refenv.reg` で戻せます。

**`analyzeHeadless` は先にプロジェクトの親ディレクトリが無いと落ちる。**
`Directory not found: C:\prog\jwwin\proj` で止まります。しかも
`launch.bat` がそこで「続行するには何かキーを押してください」を出すので、
ssh 越しだと固まったように見えます。

**Ghidra の `DIALOGEX` 判定。** `DLGTEMPLATEEX` は先頭が
`WORD dlgVer=1; WORD signature=0xffff` です。`signature` だけ見て
`dlgVer` を 1 つ後ろから読むと、ダイアログ 108 枚のうち
`DIALOGEX` のものが全部ずれて `struct.error` になります（一度やりました）。

**Ghidra は `-readOnly` でもプロジェクトを排他ロックする。** 同じ
プロジェクトに 10 個の headless を向けると、最初の 1 つだけが動いて残りは
`Unable to lock project!` で死にます。**それでも実行は `DONE` を出す**ので、
出力が 1 つしか無いことに気づくまで成功に見えます。`tools/decomp_box.ps1` は
プロジェクト（146 MB）をシャードごとに複製します。

**`CData` の共通部で、線色と線種は思う順番の逆。** `+0x28` の 1 バイトが
**線種**（点の形、文字の種類にも使い回される。だから `% 100` される）、
`+0x2a` の 2 バイトが**線色**です。逆に取ると Test1 がほぼ全部水色になります
（`CDataSolid` が `+0x2a == 10` すなわち「任意色」を見るのが決め手）。

**円弧の 7 つの倍精度は「中心 x, 中心 y, 半径, 開始角, 開き角, 傾き, 扁平率」。**
5 番目は終了角ではなく**開き角**です。終了角だと Test1 の隅の丸めが
真円になります。

**背景で撮ると作図領域が当てにならない。** `tools/shot.ps1` の既定
（`PrintWindow`）は**枠は完璧**ですが、作図領域は文字が落ちるだけでなく、
図面によっては**ほとんど描かれていない絵**が返ってきます。
日影図.jww は背景で 7,979 画素、前面で 46,283 画素でした。
撮る前に `RedrawWindow`（`RDW_UPDATENOW | RDW_ALLCHILDREN`）を投げても
変わりません。Jw_cad が作図領域を何から転送しているにせよ、`WM_PRINT` は
そこに届かないということです。
**図面を基準にするときは `-Screen`**。画面を最前面に持ち上げるので、
人が使っている最中には向きません。

**この環境ではシェル越しにバックスラッシュのエスケープが 1 段はがれる。**
Python のヒアドキュメントで C の 16 進エスケープを書こうとすると生バイトが
ファイルに入り、UTF-8 として保存されて化けます。\\
`chr(92)` でバックスラッシュを作るのが確実です
（`src/ui.c` のステータス行の文字列）。

**Git Bash から Windows のパスを渡すとバックスラッシュが消える。**
`-Exe orig\Jw_win.exe` が `origJw_win.exe` になります。スラッシュを使うこと。
日本語のファイル名は ssh / PowerShell の間で CP932 に落ちて壊れるので、
`orig/Test1.jww` のような ASCII 名で試すこと。

## 押さえた番地

すべて**イメージ内の仮想番地**です（`ImageBase = 0x00400000`）。

| 番地 | 中身 |
|---|---|
| `0x004d8ef4` | エントリポイント |
| `0x00401000` | `.text` の先頭。5,582,629 バイト |
| `0x00954000` | `.rdata`。RTTI の型記述子とロケータはここ |
| `0x009fe000` | `.data` |
| `0x00a15000` | `.rsrc` |
| `0x00954000` | インポートアドレステーブル（`0xae8` バイト） |
| `0x004d2820` | `CJw_winDoc::Serialize`（vtable スロット 2） |
| `0x00575010` | その本体。22,125 バイト。`"JwwData."` を見るのはここ |
| `0x0096b1ec` | `"JwwData."`（`.rdata`、ANSI）。UTF-16 版は `0x00962518` |
| `0x0042e690` | `CData::Serialize` |
| `0x00420650` | `CArchive::operator>>(double&)` らしきもの |
| `0x00420820` | `CArchive::operator<<(double)` らしきもの |
| `0x0042ddc0` | `CArchive::IsStoring()` らしきもの |
| `0x00a08ae4` | 座標の細工の添字（1〜10 で巡回） |
| `0x00a08ae8` | その表 |

## クラス構成

`decomp/rtti/hierarchy.txt` が全 676 クラスの継承です。Jw_cad 自身の骨格：

```
CJw_winApp  : CWinApp
CMainFrame  : CFrameWnd
CJw_winDoc  : CMiniDoc : CDocument
CJw_winView : CView, CGamenJoken          ← 多重継承。CGamenJoken が画面条件
CScreenWnd  : CWnd
CZoom, CLayer, CLType, CStyle, CGamenJoken
CData  : CObject   → Sen / Enko / Moji / Ten / Solid / Sunpou / Block / List
                      と 3D 版 3 つ
CZukei : CObject   → CZukeiObject   … 点・線・円弧・分割・中心線・2 線 …
                   → CZukeiSentaku  … 複写・移動・複線・コーナー・消去 …
```

`decomp/rtti/vftables.csv` に「クラス, vtable の番地, 何番目, 関数の番地」が
41,443 行あります。`decomp/inventory.csv` と番地で突き合わせれば、
どの関数がどのクラスのものかが引けます。

## リソースの型番

MFC が独自に使う 2 つは `afxres.h` にあります。`tools/rsrc.py` は両方解きます。

| | |
|---|---|
| 240 | `RT_DLGINIT` —— コンボボックスの初期項目。`ctrlId, msg, len, データ` の並び |
| 241 | `RT_TOOLBAR` —— `wVersion, wWidth, wHeight, wItemCount` とコマンド ID の配列。0 が区切り |

ツールバーは同じ並びが **16×15 から 120×75 まで 12 段階**入っています。
ボタンの拡大はビットマップを引き伸ばしているのではなく、大きさごとに
別のビットマップを持っているということです。移植でもそのまま使えます。

## 枠の実測（`docs/ref_start.png`、クライアント領域 1264×741）

窓は 1280×800 で起動し、クライアントは **1264×741**。
すべてこのクライアント座標です。

| | |
|---|---|
| 上のコントロールバー | y 0〜31。`CDialogBar`。中身はコマンドごとに別のダイアログ資源 |
| 画布（`CJw_winView`） | **x 78〜1185、y 34〜719**。窪み枠が y=32 に `#A0A0A0`、y=33 に `#696969` |
| 左ツールバー | x 0〜77。ボタン 2 列、幅 **31**、高さ **22**（x 3〜33 と 42〜72） |
| 右ツールバー | x 1190〜1263 |
| ステータスバー | 下端 |

Windows 11 の既定のシステム色がそのまま出ています。移植側は決め打ちでよいはず。

| | |
|---|---|
| `#F0F0F0` (240) | `COLOR_BTNFACE` 背景とボタン面 |
| `#A0A0A0` (160) | `COLOR_BTNSHADOW` |
| `#696969` (105) | `COLOR_3DDKSHADOW` |
| `#FFFFFF` | `COLOR_BTNHIGHLIGHT`、画布 |
| `#E3E3E3` (227) | ボタンの内側の枠（テーマ） |

**ボタンの絵は 1 bit のビットマップ転送で、アンチエイリアスがありません。**
拡大して確かめました。つまり**フォントを合わせなくてもツールバーは画素一致します**
——文字に見えるものは資源に焼かれた絵だからです。
選択中のボタン（起動直後なら「／」）は市松模様の地になります。

## ツールバーのビットマップの担当

`.rsrc` の `RT_TOOLBAR`(241) と `BITMAP` は**同じ id** で対になっています。
**基準 id が 24×15 で、+1 するごとに 1 段大きくなり、+9 の 120×75 まで 10 段**
あります（`464` が 24×15 なら `473` が 120×75）。
画面のボタンの大きさを変えると、引き伸ばすのではなく別の帯に差し替わります。

| 基準 id | 中身 |
|---|---|
| `464` | 新規 開く 上書 保存 印刷 切取 コピー 貼付 線属性 |
| `474` | ／ □ ○ 文字 寸法 ２線 中心線 連線 AUTO |
| `484` | 範囲 複線 コーナー 伸縮 面取 消去 複写 移動 戻る |
| `494` | 点 接線 接円 ハッチ 建平 建断 建立 多能 曲線 |
| `504` | 図形 図登 記変 座標 外変 測定 表計 距離 式計 パラメ |
| `514` | 属取 線角 鉛直 X軸 2点角 線長 2点長 間隔 基設（青） |
| `524` | 包絡 分割 整理 属変 BL化 BL解 BL属 BL編 BL終 |
| `453` | 進む ソリッド TAG 画像 中心線 上円1/4 … Test SPEED |
| `574` | ①〜⑩ |
| `401` `402` | MFC 標準（新規・開く・保存・印刷・切取・コピー・貼付・元に戻す・バージョン情報） |

左ツールバーは `494`（1 列目上）＋`524`（1 列目下）＋`474`（2 列目上）＋
`484`（2 列目下）、右は `464`＋`514`＋`504` あたりの組み合わせに見えます。
**どのバーがどの帯を何個ずつ使うかは未確定**なので、明日は実測と
`CMainFrame::OnCreate` の逆コンパイルで詰めること。

## 上のコントロールバーはコマンドごとに別のダイアログ

`CDialogBar` で、資源はコマンドごとに 1 枚です。起動直後（線コマンド）は
**`DIALOG 280`**（462×16 ダイアログ単位、フォント 9pt ＭＳ Ｐゴシック）。
中身は「矩形」「水平･垂直」「傾き」＋コンボ、「寸法」＋コンボ、「１５度毎」…。

**ダイアログ単位から画素への換算はフォントに依存します**。ここだけは
字形ではなく**配置**がフォントに引っ張られるので、基準画像から実測した
換算係数を決め打ちにするのが現実的です。

## 設定の置き場所

`.ini` ではなく**レジストリ** `HKCU\Software\Jw_cad\jw_win` です
（`AutoMode`、`View` などのサブキー）。`View\Direct2d` が描画経路の切り替え。
移植側が同じ絵を出すには、色や線幅の設定もここから読む必要があります。

## ボタンを押したときの動作

**どのボタンがどのコマンドか。** `src/gen/layout.h` が「このボタンは
ビットマップ何番の何番目の絵」と言っていて、`RT_TOOLBAR` が同じ順に
コマンド ID を並べている（セパレータは絵を持たないので飛ばす）。
`tools/mkcmd.py` が `src/gen/cmds.h` に焼く。文字列表の「説明
名前」の
名前が画面の文字（点・接線・線・矩形…）と全部一致するので、合っていることも
確かめられる。

**押下状態。** `ON_UPDATE_COMMAND_UI` のハンドラ（`FUN_00511c20`）は

    pCmdUI->SetCheck(view->現在のコマンド == id);

の一行だけ。現在のコマンドは `view+0x8564`、前のは `+0x8568`。入るのは
`FUN_004fdba0` → `FUN_004fdc40`（15,982 バイトの巨大な switch）。

**コマンドとクラスの対応**は `decomp/res/cmdclass.txt`（52 個）。
`FUN_004fdc40` の各 case が作るオブジェクトのコンストラクタが積む vftable
から引いた。線と矩形はどちらも `CZukeiSen`（`view+0x861c` を共有する二つの
モード）、円は `CZukeiEnko`、点は `CZukeiTen`。

**モードか実行か。** `ON_UPDATE_COMMAND_UI` が

    pCmdUI->SetCheck(view->現在のコマンド == id);

の一行だけならモード（押すと現在のコマンドになり、凹む）。そうでなければ
実行。その一行をやる関数は一つではない（`FUN_00511c20` は `view+0x8588` の
番人つき、`FUN_005114d0` は素）ので、名指しせず「本体が `+0x8564` と比べて
CCmdUI の 4 番目＝SetCheck を呼ぶ」で判定する（`tools/mkcmd.py`）。
70 個中 47 がモード、23 が実行。

**元に戻る**（`0xe12b`）は `FUN_00511a50` が `Enable(リストが空でない)` なので、
戻せるものがあるときだけ有効。基準画面で灰色なのはそのとき何もないから。

**新しい要素の属性**は `CData` のコンストラクタ（`FUN_0041f2d0`）のまま：

    +0x28 = 1     線種（実線）
    +0x2a = 2     線色
    +0x2c = 0     線幅
    +0x2e = doc[0x24ec + doc[0x256c] * 4]   書込レイヤ
    +0x2f = doc[0x256c]                     書込レイヤグループ

コマンドはこのあと書込線色・線種で `+0x28`/`+0x2a` を上書きする
（`param_4[0x24]` と `+0x92`、`all_00401000.c:63516` 付近）。**その置き場所は
まだ追えていない** ので、いまはコンストラクタの値のまま。

**まだやっていないこと。**

* 上の欄（コマンドバー）はコマンドごとに別のダイアログ。線と矩形は
  `DIALOG 280`（矩形・水平垂直・傾き・寸法・15度毎…）。切り替えには
  チェックボックスとコンボボックスを描く必要がある。
* 仮線の出し方。原典は `SetROP2`（`FUN_0079f1b8`）を持っているので何らかの
  ラスタ演算で重ねているはずだが、そこは追えていないので、いまはできる
  要素をそのまま描いている。
* (R) の Read（点の吸着）。
* 線・点・円以外のコマンドは、押すとコマンドが変わるだけ。プロンプトも
  分かっているものだけ（`tools/mkstr.py` の一覧）。作り話はしない。
* 円は既定（中心・通過点・真円）だけ。`CZukeiEnko` のコンストラクタが
  掃引角 2π・扁平率 1・円弧フラグ off で始まるので、そこまで。
* 元に戻るは、ここのコマンドが足した要素を外すだけ。原典の undo は
  もっと広い。

## 保存

`src/jwwrite.c`。要素リストだけを書く。**ヘッダは読んだバイトをそのまま
通す**（`jw_drawing.head`）—— 読み取りはヘッダの一部しか理解していないので、
残りを作文するわけにいかない。

**新規図面もこれで保存できます。**原典をファイルなしで起動してそのまま
「名前を付けて保存」させると、Jw_cad 自身の空図面が手に入ります
（`decomp/res/new.jww`、17,431 バイト）。`tools/mknew.py` がこれを
`src/gen/newjww.c` に焼き、`app_new` は読み直すだけ。用紙・16 のレイヤ
グループ・線色線幅・線種・ハッチと寸法の設定・文字種 10 個がぜんぶ原典の
ものになり、ヘッダも付いてきます。新規で線を引いて保存し、原典に開かせて
上書き保存させても線はそのままでした。

その空図面に入っている文字 6 個（`Printer_Orientation = 0` など）は
**保存のときに作られる設定記録**で、図面が持っているものではありません
（`FUN_005707f0` が書き、`FUN_00572880` が読み戻す）。だから `app_new` は
捨てています。残すとレイヤバーがそのレイヤを「何か書かれている」と描いて
しまい、基準画面と 64 画素ずれました。移植は書き戻しませんが、原典で開いて
保存し直せば原典が自分で付け直します。

**書込文字種は 10/10/1 色 5** でした（3.5/3.5/0 色 2 ではありません）。
原典を素で起動して 文字 で A を置かせ、保存して確かめた値で、レジストリの
`MOJI\SizeNo=10` とも一致します。

検証は二段構え。

1. `tests/write_test.c` —— 同梱 15 枚を読んで書き戻し、**バイト単位で一致**。
   ヘッダは素通しなので、これが通るのは要素リストの書き出しが原典と
   1 バイト違わないということ。`tools/check.sh` に入れてある。
2. こちらで要素を足したファイルを**原典に読ませた**。`tools/jwopen.ps1` が
   画面を奪わずに開いて保存させる。Test1.jww（1,686）に線・円・点を足した
   1,689 を原典が読み、保存し直したものが 1,695（＝1,689 + 原典が必ず足す
   設定の隠し文字 6 個）で、内訳も線 1,643・円弧 5・点 5 と合っていた。

## 追いかけて外れたもの（同じ穴を掘らないように）

**書込線色・線種の置き場所**は、**文書に無いのが答え**でした（「書込線色・
線種はファイルに入っていません」の項）。そこへ辿り着くまでに外した先を
残しておきます。

* `doc+0x5d90` / `0x5d94`（`v > 0xe1` で読む 2 つの long）は補助線の色。
  15 枚とも 0x808080 か 0xc0c0c0 で、レジストリの `Pen\ColorZl` と同じ。
* 書込レイヤグループの近く（`0x24a4`〜`0x24e0`）は誰も読み書きしていない。
* `v > 0xd3` の 21 個の long は保護（`FUN_00574a20`）まわり。
* `CDataSunpou` の `+0x90`/`+0x92` は寸法線自身のペンで、文書のものではない。

分かった文書のオフセット（`CJw_winDoc::Serialize` より。描画コードが
`param_1 + …` で触るのは、これらから 0x88 を引いた番地）：

    +0x2f98            用紙サイズの番号
    +0x24e4            書込レイヤグループ      ← 用紙サイズの直後の long
    +0x23a4 + g*4      レイヤグループの状態
    +0x2464 + g*4      そのグループの書込レイヤ
    +0x24f0 + g*8      そのグループの縮尺
    +0x17a4 + g*64 + l*4   レイヤの状態
    +0x25f4 + g*64 + l*4   レイヤの状態 2

## 文字コマンド

**手順は「先に打ってから、位置をクリック」**です。

1. `WM_COMMAND 32806` で文字コマンドに入ると、**`#32770` のダイアログが
   トップレベルに現れます**（フレームの子ではないので `EnumChildWindows`
   では見つかりません。`tmp/jwdraw.ps1` の `top` ステップで出ます）。
   中身は ComboBox 1422（文字列）・1423・Button 2261〜2265。
2. その ComboBox の Edit に `WM_CHAR` を投げて文字を入れる。
   `WM_SETTEXT` では駄目でした（原典のハンドラが見ていない）。
3. **図面をクリックすると、そこに置かれます。**Enter では置かれません。

`tmp/jwdraw.ps1 -Clicks 'cmd:32806;type::ABCDEF;300,200'` で試せます
（`type:` は打ってから Enter、`type::` は Enter なし）。

**できる要素**（`CDataMoji`）:

* `d[0],d[1]` はクリックした点そのもの。
* `d[2],d[3]` は行の終点。**送り幅は半角が `cw/2`、全角が `cw`**。
  "ABCDEF"（半角 6 文字）で 6×10=60、cw=20 の図面では 3 文字で 30 でした。
* `d[4]=cw, d[5]=ch`、`n` は書式番号。
* **書式は図面ごと**で、**ヘッダの中に見つけました**。文字種 10 個の表
  （幅・高さ・間隔の倍精度と色の long、28 バイトずつ）の**直後の 1 件が
  「いまの書式」**です。読み取りの `read_header` はもともとそこを通過して
  いて、値を捨てていただけでした（`if (v > 0x14)` の 10 回ループ）。
  Test1 は 10/10/1 色 5、Test5 は 20/20/0 色 1、Test7 は 3/3/0.5 色 2 で、
  置いた文字の値と一致します。
* 書式番号 `n` は、いまの書式が表の何番目かで、無ければ 0（任意サイズ）。
* **送り幅は `N×cw/2 + (N−1)×sp/2`**（半角。全角は cw と sp）。
  cw=10 sp=1 の図面に半角 6 文字を置くと 32.5 になりました。
* 属性取得では文字書式は変わりません（変わるのはレイヤだけ）。

**移植も原典と同じ要素を作ります。**Test1・Test5 それぞれで、レイヤ・色・
書式番号・寸法・間隔・角度・両端点まで一致（`tests/click_test.c`）。

入力は、ネイティブが `WM_CHAR` と `WM_IME_COMPOSITION` の `GCS_RESULTSTR`
（`ImmGetCompositionStringA` が CP932 のバイトをそのまま返すので変換不要）、
ブラウザが canvas の上に透明な `<input>` を重ねて `compositionend` で
UTF-16 を受け、`jw_from_utf16` で CP932 に落とします。

**入力窓も出すようにしました。**原典のを実機から読んで作ってあります
（`tmp/jwdraw.ps1` の `box` ステップ）:

* `#32770` のポップアップ。**フレームのクライアント座標で (81,39)**、
  ウィンドウ 700×65、クライアント 684×26。
* 中身は 文字のコンボ (3,1 461×26)、フォントのコンボ (471,3 120×20)、
  「フォント読取」ボタン (596,3 90×20)。

**枠とタイトルバーだけは画素一致ではありません。**テーマ付きのウィンドウ枠
（角丸とグラデーション）で、MFC が平らに描くものではないからです。
タイトルバーの色 `0x99b4d1` は `DrawCaption` をメモリ DC に描かせて取った
実際の値です。中身の位置は全部実測どおり。

## 日本語入力（入っています）

決めた方針どおりに作り、両方で漢字が打てるところまで確かめました。

* **ブラウザ**：canvas に IME は出せないので、入力位置に透明な `<input>` を
  重ねて `focus()` し、`compositionstart` / `compositionupdate` /
  `compositionend` を拾う。変換候補ウィンドウはその要素の位置に出るので、
  キャレットに合わせて置けば位置も合う。未確定文字列は
  `compositionupdate` の値を下線付きで canvas に描く。
* **ネイティブ**：`WM_CHAR` と `WM_IME_COMPOSITION`。後者の `GCS_COMPSTR` が
  未確定、`GCS_RESULTSTR` が確定で、どちらも `ImmGetCompositionStringA` が
  CP932 のバイトで返す。`-limm32` が要る。
* **内部は CP932 のまま**。`.jww` の文字列は CString の MBCS バイト列だし、
  東雲フォントも JIS X 0208 なので表示系と一貫する。ネイティブは
  `ImmGetCompositionStringA` が CP932 をそのまま返すので変換すら要らない。
* 変換表は**もうある**。`tools/mkcp932.py` → `src/gen/cp932.h`（復号 9,672・
  符号 9,274 組）と `src/cp932.c`（`jw_from_utf16` / `jw_to_utf16`）。
  v700 の図面を読み書きするのに作ったもので、そのまま使える。

## 原典自身の保存形式は v700

同梱の 15 枚は v600 ですが、**Jw_cad 10.03.6 が保存すると v700** になります。
v600 との違いは二つで、どちらも実際に保存させて突き止めました。

* **CString がすべて UTF-16。** MFC の印 `0xff 0xfffe` のあとに長さ、
  そして UTF-16。読み側はもともと印を見ていたので、足りなかったのは
  CP932 への変換だけ（`src/cp932.c`）。書き戻すときは**来たとおりの
  符号で**出すので、プールには文字列ごとに 1 バイトの目印を持たせて
  あります（`jw_str_wide`）。
* **二つのリストのあとに long が 1 つ増える。** 埋め込み画像の数です
  （`FUN_00575010` の `0x275 < version` の枝。0 でない場合は
  1 件ずつファイル名を読んで `%temp%` に展開する）。手元の図面はすべて 0
  なので、0 以外ならエラーにしてあります。

`tests/write_test.exe` は v700 の図面（原典に保存させたもの）も
バイト単位で一致します。つまり CP932 ↔ UTF-16 の表は Windows のものと
49 個の文字列ぶん完全に同じでした。

## 原典を実際に動かして確かめる（画面は撮りません）

`tmp/jwdraw.ps1` は原典を起動して図面を開き、いちばん面積の大きい
子ウィンドウ（ビュー）に `WM_LBUTTONDOWN` / `WM_LBUTTONUP` を投げて、
最後に `WM_COMMAND 57603`（上書）を送ります。キャプチャは一切しません。

**ウィンドウは出さざるを得ません。**`SW_SHOWNOACTIVATE` で出したうえで
`HWND_BOTTOM` に落とし、フォーカスは奪いません。それ以上は無理でした:

* **隠す（`SW_HIDE`）と** Jw_cad が別の大きさで自分を配置し、紙の座標が
  ぜんぶずれます。
* **画面の外や隅に動かすと** MFC がドッキングバーを組み直し、**その配置を
  レジストリに書き戻します**。次の起動で左ツールバーが 2 列から 1 列に
  なり、座標が 39 画素ずれました。`tools/refenv.sh` で戻せます。

つまり「最背面に置く」までが、原典の挙動を変えずにできる限界です。

    powershell -File tmp/jwdraw.ps1 -Cmd 32883 -Clicks '250,180;450,180;450,320'
    powershell -File tmp/jwdraw.ps1 -After 57643          # 最後に元に戻る
    -Open ''              ファイルを開かずに起動する（素の設定で出る）
    -Clicks 'cmd:32794'   途中でコマンドを切り替える
    -Clicks 'r300,200'    右クリック
    -Clicks 'c300,200'    実カーソルもそこへ動かしてからクリック
    -Clicks 'raw:v,5136,0,0'      ビューに任意のメッセージ（これは選択確定）
    -Clicks 'shot:tmp/a.png'      ウィンドウをオフスクリーンに描かせて撮る
    -Clicks 'saveas:x'            名前を付けて保存（下記）
    -Clicks 'bar' / 'box' / 'top' / 'btn:1120' / 'off:1323' / 'type:あ'
    -Clicks 'pb:1147'             BM_CLICK を投げるだけ（下記）
    -Clicks 'm400,350'            クリックせずにカーソルだけ動かす
    -Clicks 'w1197,467[,r]'       フレームのクライアント座標で押す（レイヤの格子）
    -Clicks 'dlg:32807,tmp/a.png' コマンドでダイアログを出して中身を書き出す
    -Clicks 'dlg:b1843,tmp/a.png' 同じことをバーのボタンを押して（直りました）
    -Clicks 'set:1411,12'         バーのコントロールに文字を入れる（ハッチと
                                  寸法のバーには効きます）

**`saveas:` は共通ダイアログを使います。**原典は既定では自前の
「ファイル選択」ダイアログ（リソース 290）を出し、その名前欄 id 1487 は
読み取り専用で `WM_SETTEXT` が効きません。レジストリの
`Dialog\FileCommonDialog` を 1 にすると Windows の共通ダイアログになり、
IDOK で保存できます（**使い終わったら `tools/refenv.sh`**）。名前は
既定のまま（無題.jww、または開いているファイル名）になるので、保存先は
「直前に使ったフォルダ」に出ます。上書き確認が出たら IDYES を送ります。

**`shot:` は `PrintWindow(PW_RENDERFULLCONTENT)`。**画面は読みません。
Direct2D が切ってある（`View_Direct2d = 0`）ので中身もちゃんと出ます。
これで「選択された要素は ff00ff、範囲の枠は ff0000」のような、
保存では分からないことが確かめられます。

**クリックが効かないコマンドがあります。**ビューのボタン処理のなかには
`GetCursorPos` で実カーソルを読むものがあり、そこは `c` 付きでないと
通りません。寸法の 3 段目はそれでも通りませんでした（「寸法」の項）。

**これで分かったこと。**

* **矩形は 4 本の `CDataSen`。** 最初にクリックした角から x 方向 → y 方向
  → 戻る、と一周し、各線は前の線の終点から始まります。クリックの順を
  逆にしても同じ形なので、**絶対座標ではなく最初のクリック基準**です。
  ファイル形式に矩形はありません。
* **連続線は 1 本分ためる。** n 回クリックして保存すると **n−2 本**。
  2 回で 0 本、4 回で 2 本、5 回で 3 本。最後の 1 本は画面に出ているだけで、
  次のクリックが来て初めて図面に入ります。
* **元に戻るはコマンド単位。** 矩形を描いて `-After 57643` すると 46 本に
  戻りました（4 本まとめて）。要素 1 つずつではありません。
* **消去は右クリックで図形消去、左クリック 3 回で部分消し。**
  矩形の一辺の上で右クリックすると 50 本が 49 本になり、消えるのは
  その辺でした。左クリックは 1 回目が図形の選択、2・3 回目が消す範囲で、
  3 回目で初めて実行されます（だから 1 回や 2 回では何も起きません）。
* **原典と同じ座標を出すところまで確かめました。** 画面 y=300 に線を引き、
  (400,300) で選んで (350,330)・(450,270) で切ると、原典は
  `-263.230,37.233 → -176.641,37.233` と `-90.052,37.233 → 39.831,37.233`
  の 2 本を残します。移植も**同じ数字**になりました（`tests/click_test.c`）。
  つまり範囲の 2 点は線に**垂線の足として投影**されます。
* **ビューの原点差は (78, 34)。** 原典の描画子ウィンドウのクライアント座標に
  78 と 34 を足すと、この移植のフレームバッファ座標になります。倍率は
  同じ（Test5 で 1.154882 画素/mm）なので、`cmd:32835`（全体表示）を
  送ってから座標を計算すれば移植側で当たりを予測できます。
  実カーソルを重ねる必要もないので、`c` 付きのクリックはもう要りません
  （ビューは `GetCursorPos` も読んで `view+0x23d0` に置きますが
  〈`FUN_00517290`〉、拾い上げに使うのは lParam の方〈`+0x23d4`〜〉です）。

## 原典に描かせた答え（`decomp/res/*.jww`）

検査の半分は「原典に描かせたファイル」と突き合わせます。`decomp/` は
リポジトリに入らない（機械出力なので）ため、**新しい環境では撮り直しが
要ります**。どれも `tmp/jwdraw.ps1` の 1 回の実行で作れて、最後に
`saveas:x` で保存させ、`tmp/rect.jww` を `decomp/res/` へ写します
（`saveas` の前には `Dialog\FileCommonDialog` を 1 に、あとで
`tools/refenv.sh`。「原典を実際に動かして確かめる」の項）。

| ファイル | 作り方（`-Open tmp/rect.jww` は `orig/Test5.jww` の写し） |
|---|---|
| `new.jww` | `-Open '' -Clicks 'saveas:x'` —— ファイルを開かずに起動してそのまま保存。**新規図面のひな型**（`tools/mknew.py`） |
| `sunpo.jww` | `-Cmd 0 -Clicks '300,600;700,600;cmd:32847;c400,500;c400,450;c300,600,r;c700,600,r;saveas:x'` |
| `poly.jww` | `-Cmd 32894 -Clicks 'ch:1413,8;ch:1411,3000;ch:1414,30;500,400;saveas:x'` |
| `mentori.jww` | `-Cmd 0 -Clicks '300,600;700,600;700,600;700,300;cmd:32859;ch:1411,2000;c500,600;c700,450;saveas:x'` |
| `bunkatsu.jww` | `-Cmd 0 -Clicks '300,300;700,300;350,600;900,700;cmd:32867;ch:1411,3;c500,300;c600,650;saveas:x'` |
| `nisen.jww` | `-Cmd 0 -Clicks '300,400;900,400;cmd:32892;ch:1412,2000,1000;c600,400;500,400;800,400;saveas:x'` |
| `nisen2.jww` | 同じで、基準線を `900,400;300,400` と逆に引き、点を `500,350;800,350` に |
| `chushin.jww` | `-Cmd 0 -Clicks '300,300;700,300;300,600;700,600;cmd:32873;c500,300;c500,600;400,450;800,450;saveas:x'` |
| `chushin2.jww` | 同じで、2 本目を `300,600;900,450` に（交わる 2 本） |

`docs/ref_zoku.png` と `decomp/res/zoku.txt`（線属性ダイアログ）は
`-Clicks 'dlg:32807,docs/ref_zoku.png'` の 1 回で両方出ます。

## 書込線色・線種はファイルに入っていません

**探しても見つからなかったのは、無かったからでした。**

原典で **属性取得**（`WM_COMMAND 32931`）を使って線種 6 の線からペンを取り、
そのまま保存して、保存前のファイルと突き合わせると、**ヘッダで変わるのは
書込レイヤとレイヤの状態だけ**でした（オフセット 66・82・162）。ほかに
変わるのは作図時間らしい値（0x97e の word と 0x9ca のバイト）と、
操作のたびに増える 0x129a の long だけです。

決定的なのは開き直したとき: `tools/refenv.sh` でレジストリを戻してから
その保存ファイルを開いて線を引くと、**線種は 1 に戻ります**。書込レイヤは
0 のまま戻ってくるのに、です。つまり書込ペンは**セッション内だけの状態**で、
起動のたびに線種 1・線色 2 から始まります。移植の動きと同じです。

変える手段として **属性取得** を入れました（0x80a3）。図形を左クリックすると
その線種・線色・線幅が書込ペンになり、レイヤも書込レイヤになります。
原典で線種 6・レイヤ 0 の線から取って複線すると 線種 6・レイヤ 0 で出る、
というところまで突き合わせ済みです（`tests/click_test.c`）。
`ON_UPDATE_COMMAND_UI` が無いのでボタンは押下表示になりませんが、
コマンドとしては入ります。

## ブラウザで文字を打たせるまでに踏んだ穴

動くまでに四つ躓いたので書いておきます。どれも「打っても何も起きない」と
いう同じ見え方をするので、切り分けの順番が大事でした。

1. **`jw_key` などを `EXPORTED_FUNCTIONS` に入れ忘れていた。** C を足した
   だけでは wasm から出てこない。ネイティブは同じ C を直接呼ぶので気付け
   ない。`node -e "require('./jwcad.js')().then(m => ...)"` で
   `typeof m._jw_key` を見るのが早い。
2. **新規図面に文字書式が無かった。** 幅 0 では置かない実装なので黙って
   落ちる。`app_new` に原典の既定（W=3.5 H=3.5 D=0 色 2）を入れた。
3. **`app_key` が絵を作り直していなかった。** ネイティブは呼び出し側で
   `app_paint` していたので動き、ブラウザだけマウスを動かすまで出なかった。
   いまは `app_key` / `app_compose` の中で作り直す。
4. **`jwcad.js` がキャッシュされていた。** wasm は `locateFile` で毎回
   新しく取りに行くようにしていたので、**新しい wasm ＋ 古い糊**という
   いちばん分かりにくい状態になった。`?v=` を上げるのと、糊に必要な関数が
   無ければ赤い帯で言うようにした。

**IME の未確定文字列は自分で描く必要があります。**原典の入力窓は本物の
Edit なので未確定も勝手に出ますが、こちらは自前描画なので
`compositionupdate`（ネイティブは `GCS_COMPSTR`）を拾って下線付きで描いて
います。これが無いと「IME をオンにすると Enter まで何も出ない」ように
見えます。

`?debug=1` を付けて開くと、右下に build 番号と、文字コマンドの状態
（focus の有無・変換中の文字列・確定した文字列）が出ます。

## コマンドバー —— 実機から読み出す

上の欄は `CDialogBar` なので、載っているのは普通の Windows コントロール
です。位置はダイアログテンプレート（ダイアログ単位）とダイアログフォント
から Windows が決めるので、**原典に聞くのがいちばん確かです**。

`tmp/bars.ps1` が Jw_cad を起動し、コマンドを順に送って、
`EnumChildWindows` でフレームの子ウィンドウを歩き、クラス・id・スタイル・
文字列・矩形（フレームのクライアント座標）を書き出します。
**画面は一切撮りません**（ウィンドウは出しますが読み戻しません）。

    powershell -File tmp/bars.ps1 -Cmds '32771,32773,...' -Out decomp/res/bars.txt
    python tools/mkbars.py            # → src/gen/bars.h

出てきた値は、参照画像から手で測ってあった定数を**すべて再現**しました
—— 矩形チェックが x=18 y=11、コンボが 185 と 319、端部点ボタンが
x=517 y=5 h=24。だから信用できます。いま 25 コマンドぶん入っています。

ついでにダイアログ単位→画素の換算も確かめられました:

    x = MulDiv(dlu, 7, 4) + 6        y = MulDiv(dlu, 12, 8) + 5
    w = MulDiv(dlu, 7, 4)            h = MulDiv(dlu, 12, 8)

（コンボの閉じた高さだけは 20 画素固定。）実測 9 点すべてに一致します。

ラベルは**コントロールの矩形に対して縦中央**に描いています。移植の字は
原典より背が高いので、そうしないと下 2 行が枠の文字領域からはみ出して
採点に出てしまいます。字が横に長くて隣とくっつくのは、フォントの違いと
いう約束のうちです。

## コーナー処理と線伸縮

どちらも原典に実際にやらせて、座標が**小数 3 桁まで一致**するのを
`tests/click_test.c` に焼いてあります。

**コーナー処理（0x8012、`CZukeiCorner`）**

* 左クリックで 2 本を指示。**クリックした側の切れ端が残り**、もう一方の
  端が交点に移ります。交点が線の外なら伸び、中なら縮みます。
* 2 本とも**その場で書き換わります**（作り直しではない。原典でも要素の
  位置が変わりませんでした）。
* ピックは `FUN_0044a270(3,…)`。2 本目のときだけ `view+0x82bc` を反転させて
  いて、同じ距離なら**後の要素**が勝つようになります（同じ線を 2 度
  拾わないため）。移植は `jw_pick_tie(…, 1)`。
* プロンプトは `FUN_00635ae0` —— 状態 0 で 5340「線（Ａ）指示(L)　線切断(R)」、
  1・2 で 5341。**右クリックの線切断はまだ**です。

**線伸縮（0x8017、`CZukeiShinshuku`）**

* 左クリックで線、もう一度で移動先。移動先は線に**投影**されます。
* **動くのは「移動先に近い方」の端**で、最初にクリックした側ではありません
  （左半分をクリックして右端の先を指すと、右端が動きます）。同点なら始点側。
* プロンプトは `FUN_00717df0` —— 状態 0 で 5336、1・2 で 5338。
  **(R) 線切断と (RR) 基準線指定はまだ**です。

## 複線（0x8020、`CZukeiFukusen`）

* **3 クリック**。1 回目で線を拾い、2 回目で向きと距離、3 回目で作図。
  3 回目は確定だけで、位置は**2 回目のまま**です（3 回目を遠くにしても
  複製は動きませんでした）。
* 距離は 2 回目のクリックの**線からの符号付き垂線距離**。
* **複製は元の線の属性を継ぎません。**Test1 の線種 6・レイヤ 0 の線を
  複線したら、線種 1・レイヤ 10（書込レイヤ）で出ました。つまり
  `jw_add()` がそのまま使えます。
* プロンプトは 5305 →（2 回目以降）5366。
  **(R) の「前回値」「複線方向反転」はまだ**です。

## 読取（(R)Read）

どのプロンプトも末尾が「(L)free  (R)Read」です。**右ボタンは読取点に
吸着**します。何が読取点かは、点コマンド（右クリックすると読んだ場所に
点が落ちる）を探針にして原典から測りました。

| 読む | 読まない |
|---|---|
| 線の端点 | 線の中点 |
| 既にある点 | 円の中心 |
| 線どうしの交点 | 全円の始点（円弧の端点は読む） |

**近くに読取点がなければクリックは何も起こしません**（点も落ちません）。

**許容はマンハッタン距離 10 画素**。角から (7,3) と (10,0) は読み、
(6,5) と (11,0) は読みません。丸い許容ならこの 4 つは揃いません。
移植では距離を**画素に丸めてから**比べています。紙のミリに直して
比べると (10,0) がちょうど境界で 4 ulp 外れるからです。

`tests/read_test.exe` が原典の答えをそのまま持っています（5 場面）。

**線×円弧・円×円の交点も読みます**（原典で確認済み。円の中を通る線の
2 交点、重なった 2 円の交点、どちらもそのとおりに落ちました）。
つぶれた円（扁平率≠1）どうしの交点だけは入れていません。

## 水平・垂直 —— 線コマンド中に 線 を押すと切り替わる

**解決しました。**原典で斜めの線が引けなかったのは、こちらが
`WM_COMMAND 32771` を「もう線コマンドなのに」もう一度送っていたからです。

`FUN_004fdc40` の 0x8003 の枝にこう書いてあります。

    if (view+0x8568 == 0x8003) {        /* 直前のコマンドも線だった */
        iVar2 = FUN_004fcd80();
        local_e428 = (iVar2 == 0);      /* 反転した値 */
        FUN_005bec00();                 /* +0x730 = +0x13bc（0x5bec3f） */
    }

`view+0x8568` は**出ていく方のコマンド**で、コマンドに入るときに
`+0x8564` から写されます。矩形（0x8004）の枝に同じ判定はないので、
**切り替わるのは 線 だけ**です。

実際に動かしても同じでした —— 起動直後の 30 度のドラッグは 30 度、
線 を 1 回押すと水平、2 回押すとまた 30 度。

* 拘束は**長い方の軸が残る**。ちょうど 45 度なら垂直。
* コマンドバーの `BM_GETCHECK` は起動直後 0、線 を押すと 1。
  `docs/ref_start.png` がチェックなしなのは正しい状態です。
* 矩形コマンドでは 1332（矩形）が 1、1333（水平･垂直）が 0 になります。
  移植もこの 2 つだけは描き分けるようにしました。
* チェックの「レ点」は **Windows 自身に描かせて**取りました
  （`tmp/dfc2.c`。`BS_AUTOCHECKBOX` を作って `WM_PRINTCLIENT` で
  メモリ DC に印刷するだけで、ウィンドウは一度も表示しません）。
  枠の左上から見て行 3〜9・列 3〜9 に入ります。

**教訓**: 原典を駆動するとき、**いま入っているコマンドをもう一度
送らないこと**。斜めの試験図形は円か連続線でも作れます。

## 消去

**右クリックで図形消去、左クリックで部分消し。**原典の文字列 10111 に
そう書いてあります ——「線・円マウス(L)部分消し　図形マウス(R)消去」。
実際に動かしても同じでした。右クリックの方だけ入れてあります。

* 許容範囲は `view+0x8ed8` の**画面 10 画素**（既定。設定で 5〜30）。
  紙のミリに直すのが `view+0x8ed0` で、
  `trunc(画素*10)/10 × 画面ミリ毎画素 ÷ 倍率`。96 dpi なら画素/倍率。
* クラスごとの距離は vtable の `+0x44`。**ユークリッド距離ではありません**。
  線（`FUN_0042c100`）は傾きの緩い軸方向のずれ、点（`FUN_0042d990`）は
  マンハッタン距離の**半分**、円弧（`FUN_0042b7d0`）は半径からのずれに
  掃引の外なら端までの弧長を足したもの。全部 `src/pick.c` にそのまま。
* 選び方（`FUN_0044a270`）はいちばん小さいものを採り、`id != 0` なら
  +1e-6、`CDataSolid` なら +3e-6 して、`< 最小 − 1e-7` で置き換え。
  文字は本体のループでは飛ばし、**何も見つからなかったときだけ**
  別の探索（`FUN_004578c0`）で拾います。
* `CZukeiShoukyo` の vtable は 6 が入口、10 が左ボタン、
  **11 が右ボタン**（ここで `FUN_0044a270(0,…)`。移植もこの**モード 0**を
  使っています）、3 が実行、9 が部分消しの 3 段階の状態機械
  （`+0x21c` が 0→1→2→3。ピックは**モード 3**）。`+0xb38`
  （`FUN_00710790`）はコマンドバーのスイッチで、立っていると左クリックが
  「節間消し」になり（文字列 10156）、1 回のクリックで実行されます。
* ビューの `RBUTTONUP`（`FUN_0050d3f0`）はコマンドの vtable `+0x2c`
  （スロット 11）を、`LBUTTONUP` は `+0x24`（スロット 9）を呼びます。
* 部分消しの実体は `FUN_00470200` →
  線なら `FUN_00471e40`、円弧なら `FUN_004702f0`、点は何もしません。
  線の方は写しました（`src/cmd.c` の `cut_line`）。両端とも残るなら 2 本に
  分かれ、片側だけなら縮み、全部入っていれば消えます。しきい値は 1e-7。
* **円弧も同じ形**（`cut_arc`）。ただし**角度しか見ません** —— 範囲の 2 点を
  円の内側でクリックしても切れる場所は同じでした。全円は 1 本の円弧が
  残り（2 点目から長い方へ）、円弧は 2 本に割れます。原典の答え:
  全円を 90°〜180° で切ると `a0=π, sw=3π/2`、それをさらに 270°〜0° で切ると
  `a0=π, sw=π/2` と `a0=0, sw=π/2`。
* **要素はその場で書き換わり**、2 本目だけが末尾に足されます（原典も同じ）。

## 範囲選択・複写・移動（入っています）

原典に同じことをさせて決めました。

* **箱に丸ごと入った要素だけ**が選ばれます。Test5 に箱をかけて保存させた
  ファイルで、またいだだけの線 11 本は 1 本も選ばれておらず、中に収まった
  26 本は全部選ばれていました。
* **終点のボタンで文字が決まります。**文字列 5326「選択範囲の終点を指示
  して下さい (L)文字を除く (R)文字を含む」のとおりで、同じ run で箱の中に
  あった文字 28 個は左クリックでは 1 つも選ばれませんでした。
* 選択は要素自身の **+0x44 のビット 1**。保存するとファイルに残るので、
  これが確かめる手段になります（`tools/jww.py` の `flags`）。
* **選択確定はコマンドバーのボタン id 1120。**ボタンの処理 `FUN_005d9de0` は
  ビューに私用メッセージ **0x1410** を送っているだけなので、原典を駆動
  するときは `tmp/jwdraw.ps1` の `raw:v,5136,0,0` が確実です。
* **基準点は確定を押したときのマウス位置**（バーの表示も
  ≪基準点：マウス位置≫、文字列 6144）。確定後に 3 回クリックさせると
  複写が 3 つでき、ずれはどのクリックとも違う 1 点から 1 倍・2 倍・3 倍
  でした。その点はカーソルが置かれていた場所です。
  **前の引き継ぎに「確定後にクリックしても複製できない」と書いたのは
  誤りでした。**できます。基準点がカーソル位置なので、ずれが予想と
  違ったのを「できていない」と読み違えていたようです。
* **色**は Pen/Color10 = ff00ff（選ばれた要素）と Color11 = ff0000
  （範囲の枠）。ウィンドウをオフスクリーンに描かせて数えました。
* **範囲選択 → 選択確定 → 消去** で、選ばれていた線 25 本がそのまま消え、
  選択も解けました（Test5 の 46 本が 21 本に）。移植も同じにしてあります。

移植は `src/cmd.c`。段は 0 始点 → 1 終点 → 2 選択済み → 3 配置中
（範囲選択だけは 4 で止まる）。`tests/sel_test.exe` が 30 項目回ります。

**まだなのは**：切取り選択・範囲外選択・前範囲・属性選択・基準点変更・
倍率・回転、そして確定後にバーが次の段（作図属性・任意方向・数値位置…）に
変わること —— 原典のそのバーはまだ読み出していません。

## 寸法（入っています、0x804f `CZukeiSunpo`）

段ごとの文字列は原典に入ってクリックのたびにステータス行を撮って読みました。

| 段 | | |
|---|---|---|
| 入った直後 | 5329 | `[寸法] 引出し線の始点を指示して下さい。(L)free (R)Read` |
| 1 回押すと | 5330 | `■ 寸法線の位置を指示して下さい。(L)free (R)Read` |
| 2 回押すと | 5331 | `○ 寸法の始点を指示して下さい` |
| その次 | 5332 | `● 寸法の終点を指示して下さい。` |

**後ろの 2 つに「(L)free」が付いていないのは抜けではありません。**その段は
**読み取れる点の上でないとクリックが何も起きません**。線の端点の上なら
左でも右でも進み、何もないところでは何回押しても進みませんでした。
（ここで長く詰まりました。空の図面で試していたのが敗因です。）

できるのは普通の要素 6 つ。原典に 1 本描かせて読んだものです:

* **寸法線** `CDataSen` 色 `SENColor`、flags 0x2000。測った 2 点を
  寸法線の高さに落としただけで、出っ張りはなし。
* **端部の点** `CDataTen` を両端に 2 つ。色 `TENColor`、flags 0x0040。
  `Arrow`=0 なので点で、1 なら矢印（未実装）。
* **引出線** 2 本。色 `HIKIDASHIColor`、flags 0x2000。寸法線から
  「引出し線の始点」の高さまで。`TSUKIDASHI`=0 なので寸法線からの
  出っ張りなし。
* **寸法値** `CDataMoji`。文字種 `MOJINO`=2（2.5/2.5/0 色 1）、寸法線の
  `HANARE`=0.5 上、中央揃え、flags 0x4010、+0x2c に 0x2043、+0x28 に 2
  （普通の文字は 1）。

値は**紙のミリ × そのレイヤグループの縮尺**。小数 `SHOUSUUKETA`=2 桁、
`ShowComma`=1 なので 3 桁ごとにカンマ、`ShowZero`=0 なので末尾の 0 は落ちる。
原典は 346.3557mm を 1/200 で `69,271.14` と書き、移植も同じ文字列。
設定は `tools/mksunpo.py` がレジストリの `[SUNPO]` から焼きます。

`tests/sunpo_test.exe` が、原典の描いた寸法（`decomp/res/sunpo.jww`）と
同じ 4 クリックを移植にさせて、**6 要素の座標が 1e-9 まで、色・フラグ・
文字まで一致**することを確かめます。

**傾きは任意の角度です。**角度はバーの **傾き の欄（ComboBox 1411）**に
入っていて、**0ﾟ/90ﾟ のボタン (1059) はその欄に 0 か 90 を書くだけ**でした
（押しながら `read:1411` で確かめました）。移植も欄ひとつを見るように直し、
30 度で原典に引かせたもの（`decomp/res/sunpo30.jww`）と**6 要素すべてが
1e-9 まで、文字も `59,990.56` まで一致**します。`sunpo_make()` は元から
回転した座標系で組み立てていたので、角度の出どころを変えるだけでした。

`tests/sunpo_test.c` は 0 度と 30 度の 2 通りを回します。要素は**原典が
作った順**（測った線・寸法線・端部 2・引出線 2・寸法値）で見分けるように
しました —— 「水平なら寸法線」では斜めのときに効きません。

## 線属性のダイアログ（入っています、0x8027）

`tmp/jwdraw.ps1` の **`dlg:<コマンド>,<png>`** 段で、原典にダイアログを
出させて子ウィンドウを全部書き出し（`decomp/res/zoku.txt`）、そのまま
オフスクリーンに描かせて絵にしました（`docs/ref_zoku.png`）。
`tools/mkzoku.py` が `src/gen/zoku.h` を作ります。同じ手が他の
ダイアログにも使えます。

読み取った見た目:

* ダイアログの地は **0xf0f0f0**（テーマの色）ですが、**色と線種のボタンは
  Jw_cad の自前描画で 0xc0c0c0、影は 0x808080** —— 昔の Windows の灰色。
* 選ばれているボタンは**上と左だけ黒、四辺に影、中身が 1 画素ずれ、左に
  チェック**。選ばれていないものは**四辺が黒、右と下の内側に影**。
* 見本は色が **32×4 を (16,6)**、線種が **33×1 を (16,8)**。
* **Ok は既定ボタン**なので 0x646464 の輪が 1 本多く、キャンセルにはない。
* 下の白い箱（選んだ線の見本、枠 34,218 136×24、線は y=230 の x=46..158）と
  右の灰色の箱（176,220 89×19、中の帯 186,229 72×2）は**コントロールでは
  なくダイアログが自分で描いています**。
* **補助線色の見本は図面のペン表ではなく設定の色**。Test5 を開いていても
  ピンク（`Pen/Color09`）で、Test5 自身のペン 9（灰）ではありません。

`tests/zoku_test.exe` が動作を見て、絵は `docs/ref_zoku.png` と突き合わせ、
**文字と窓枠を除いて 18 画素差**（補助線種の見本の位相が 1 画素ずれている
だけ）。

## レイヤとレイヤグループの格子（入っています）

原典で押して保存させ、ファイルに残る状態から決めました。

* **右クリックで書込**（3）になり、それまで書込だったものは 編集可（2）に
  落ちる。レイヤグループも同じ。
* **左クリックは 編集可(2) → 非表示(0) → 表示のみ(1) → 編集可** と回る。
  レイヤ 6 を 3 回押すと元に戻り、4 を 2 回で 表示のみ、3 を 1 回で 非表示。
* **書込レイヤは左クリックしても動きません。**

原典側の格子はボタン（id 33038）の集まりで、フレームの子をたどらないと
押せません。`tmp/jwdraw.ps1` の **`w<x>,<y>[,r]`**（フレームのクライアント
座標で押す）を使います。`WindowFromPoint` では手前のウィンドウが返って
くるのでだめでした。

## コマンドバーの数値欄（入っています）

残りのコマンドの多くは、バーの欄に数値が要ります。**原典にそれを渡す
手も分かりました**: `tmp/jwdraw.ps1` の **`ch:<id>,<文字>`** が本物の
`WM_CHAR` を 1 文字ずつ送ります。`WM_SETTEXT`（`set:`）では**コマンドが
読み直さないので効きません**。これで多角形・面取・分割・２線が
確かめられました。

移植側も、バーのコンボを押すとそこにカーソルが移り、数字・小数点・
マイナス・カンマ・バックスペースを受け取り、Enter で確定します
（`jw_cmd_box*`）。欄はコマンドごとに別（id はバーをまたいで使い回されて
いるため）。ブラウザ側は `jw_box_focus` を見てキーを振り分けます。

## 多角形（入っています、0x807e）

中心→頂点指定（既定）で、**クリック 1 回**。原典に 角数 8・寸法 3000・
底辺角度 30 を打ち込ませて読みました。

* **外接円の半径 = 寸法 ÷ レイヤグループの縮尺**（3000 を 1/200 で 15mm）。
* **頂点は −90° − 180/n + 底辺角度 から 360/n きざみ** ——
  底辺が水平で、そこから底辺角度だけ回る。五角形（既定）と八角形の
  両方がこの式でした。
* 既定値は 寸法 1000・角数 5・底辺角度 0（レジストリではなくコマンドが
  持っている値）。

## 面取（入っています、0x805b、角面・辺寸法だけ）

線を 2 本選ぶ。**交点から各線に沿って 寸法÷縮尺 だけ戻ったところで切り、
その 2 点を結ぶ線が 1 本増える**。残るのはクリックした側。切った 2 本は
**切り口の点を先に**書き直される。寸法の欄が空なら何もしない。

## 分割（入っています、0x8063、等距離分割だけ）

線を 2 本選ぶ。**分割数 n で n−1 本**が間に入り、位置は両端どうしを結んで
k/n（始点は始点と、終点は終点と）。平行でも同じ長さでもない 2 本で
確かめました。

## ２線（入っています、0x807c）

基準線を選び、2 点を打つ。**その 2 点の線に沿った範囲**に、間隔
"a,b" の 2 本が**基準線の向きで決まる側**にできる（クリックした側とは
無関係。線を逆向きに引くと入れ替わります）。留線は未実装。

## 中心線（入っています、0x8069）

段は 4 つ: 1 番目の線 → 2 番目の線 → 始点 → 終点。**平行なら真ん中、
交わるなら二等分線**で、打った 2 点はその線に下ろした垂線の足になります。

## 窓の見た目（追従・キャプション・メニュー）

**追従。**原典を 1264x741 と 1484x841 で動かし、子ウィンドウの位置を全部
書き出して比べました（`tmp/jwdraw.ps1` の `size:` と `all`）。動くのは
2 つだけです:

* **右のコントロールバーとその中身**（ツールバー・レイヤの格子・その下の
  ボタン）は**幅の差だけ右へ**。上からの位置は変わりません。
* **ステータス行**は**高さの差だけ下へ**、幅はクライアント一杯に。
* 上のバーと左のバーは位置そのままで、幅・高さだけ伸びます。
* ほかは 1 つも動きません（コマンドバーの中身も左のツールバーも）。

移植は `ui_right` / `ui_bottom` と、バーの表に付けた 4 つの印
（`A_RIGHT` / `A_WIDE` / `A_TALL` / `A_BOTTOM`）。当たり判定も同じ式を
通します。検査は `tools/mkmask.py` が文字のマスクを同じ規則で移して
1484x841 で突き合わせ、**0 画素差**です。

**キャプションとメニューバー。**ブラウザには窓がないので移植が描きます
（ネイティブは Windows からもらいます）。採寸は `docs/ref_window.png`
—— 原典の窓を `PrintWindow` でビットマップに描かせたもの:

* 0〜30 行がキャプション（0xf3f3f3）。アイコン 8,7、名前 30,10、右の
  3 つのボタンの字は右端から 119・74・28 画素、幅 10。
* 31〜50 行がメニューバー（白。最後の 2 行が 0xf2f2f2 と 0xf0f0f0）。
  名前は 8, 70, 136, 189, 257, 309, 378 から、上から 4 行目。

**アイコンは原典のもの**です。`Jw_win.exe` の RT_ICON 14 個のうち **2 番**
（16x16・4 ビット）がキャプションと 256 画素すべて一致しました
（`tools/mkicon.py`）。メニューの名前と木は `tools/mkmenu.py` が
メニュー資源から拾います。

クライアント側の座標は動かしていません。ブラウザ用の縦積みは
`app_chrome(1)` で、`app_rgba()` がキャプション＋メニュー＋クライアントを
1 枚に並べ、`jw_press` / `jw_move` が y からその高さを引きます。

## 拾い上げ（`src/pick.c`）

`FUN_0044a270` と各クラスの vtable `+0x44` をそのまま写したものです。

* 許容範囲は `view+0x8ed8` の**画面 10 画素**（既定。設定で 5〜30）。
* **距離はユークリッドではありません。**
  * 線（`FUN_0042c100`）: 傾きの緩い軸方向のずれ。端より外なら はみ出しを足す。
  * 点（`FUN_0042d990`）: マンハッタン距離の**半分**（だから線より優先される）。
  * 円弧（`FUN_0042b7d0`）: 半径からのずれ。掃引の外なら端までの弧長を足す。
    全円かどうかの判定に使う定数は 2π ではなく **6.283185207179586**。
  * 文字（`FUN_0048a1e0`）: 外接矩形の 4 辺を `CDataSen` として測る。
    幅は原典はフォントで測りますが、こちらはファイルが持つ両端の距離。
  * ソリッド（`FUN_0042c630`）: 4 隅のマンハッタン距離、次に 4 辺
    （線と同じだが端の外は考えない）。
* 選び方: `id != 0` なら +1e-6、ソリッドなら +3e-6 して、`< 最小 − 1e-7`。
  文字は**他に何も近くになかったときだけ**、モード 1 未満で探します。
* **原典と突き合わせ済み。**`Ａマンション平面例.jww` を全体表示して
  消去の右クリックを 3 か所に打つと、1,394 本のうち移植が予測した
  ちょうどその 3 本だけが消えました。`Test7.jww` の文字も 1 つだけ
  一致。`tests/pick_test.exe` が 15 枚で回ります。
